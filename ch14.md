# 제14장. 시점과 가격 움직임 — 시차 알파의 데이터 토대

## 들어가며

13장 끝에서 나는 한 가지를 적어두고 닫았다. *대한광통신이 1년 먼저 움직였고, 광전자가 4월 1일에 갭 상승했고, 이노인스트루먼트가 4월 7일에 첫 상한가를 친 그 시간의 차이가 데이터로 박혀 있어야 한다.*

이 장이 그 약속을 짓는 자리다. *시점*이 데이터 모델 안에서 *일급 시민*이 되는 자리.

지금까지 데이터에 들어간 시점 정보는 — *공급 시작 연도*, *제품 출시 연도*, *기술 상용화 연도* 같은 *대략적인 시간*이었다. 그러나 *시차 알파*가 작동하려면 — 훨씬 더 정밀한 시점이 필요하다. *어떤 회사가 어느 날 어떤 가격에 있었는가*, *어느 시점에 큰 움직임이 시작됐는가*. 일 단위, 때로는 그 이하의 정밀도.

이 장의 끝에서 1편 회상의 세 회사 — 대한광통신, 광전자, 이노인스트루먼트 — 의 *결정적 가격 움직임*이 데이터에 박힌다. 그리고 — *대장주가 누구인지*가 데이터 안에서 자동으로 도출된다. 15장의 본격 *시차 알파 분석*을 위한 모든 토대가 14장에서 완성된다.

---

## 14.1 `significant_price_move` 관계 — 시점이 일급 시민이 되는 자리

가격 움직임을 데이터로 적는 방식은 두 가지가 있다.

**첫째, 모든 일별 종가를 다 적는 방식.** 한 회사의 한 해 종가 250개, 다섯 해라면 1,250개. 회사 100개라면 12만 5천 개의 데이터 포인트. 단순 통계 분석은 이 방식이 자연스럽다. 그러나 — *시차 알파 분석*에는 이런 dense 데이터가 *불필요한 정밀도*다. 더 큰 문제는 *의미가 가려진다*는 점이다. 250개의 종가에서 *어디가 결정적 움직임이었는가*는 — 그 데이터 안에 *명시적으로 적혀 있지 않다*.

**둘째, 결정적 움직임만 사건으로 적는 방식.** *언제 큰 갭 상승이 있었는가, 언제 처음 상한가를 쳤는가, 언제 신고가에 도달했는가*. 의미 있는 사건만 — *비교적 sparse한* 데이터로. 이 방식이 — *시차 알파 분석*에는 훨씬 자연스럽다. 그리고 — *그 자체로 분석가의 해석을 담은 데이터*가 된다. *어디가 결정적 움직임이었는가*에 대한 판단이 데이터에 명시적으로 박힌다.

이 책은 두 번째 방식을 채택한다.

```typeql
transaction schema optical_chain
```

```typeql
define
  relation significant_price_move,
    relates moving_company @card(1..1),
    owns move_date @card(1..1),
    owns move_type @card(1..1),
    owns price_change_pct @card(0..1),
    owns price_before @card(0..1),
    owns price_after @card(0..1),
    owns trigger_event @card(0..1);
  
  company plays significant_price_move:moving_company;
  
  attribute move_date, value date;
  attribute move_type, value string 
    @values("gap_up", "consecutive_upper_limit", "breakout", "new_high", "first_significant_rise");
  attribute price_change_pct, value double;
  attribute price_before, value double;
  attribute price_after, value double;
  attribute trigger_event, value string;

commit;
```

여기서 결정적 자리는 — `move_type`의 `@values`이다. 다섯 가지 사건 종류가 강제된다.

- `gap_up` — 갭 상승. 전날 종가보다 크게 위에서 시가가 형성됨.
- `consecutive_upper_limit` — 연속 상한가. 한국 시장의 특수한 사건. 며칠 연속으로 상한가에 도달.
- `breakout` — 박스권 돌파. 일정 기간의 가격 범위를 깨고 상승.
- `new_high` — 신고가. 과거 최고점을 갱신.
- `first_significant_rise` — 첫 의미 있는 상승. 장기 동전주에서 처음으로 큰 상승이 시작된 자리.

이 다섯 종류가 *시장 사건의 의미 단위*다. *움직였다*가 아니라 — *어떤 종류의 움직임이었는가*가 데이터에 박힌다. 7장의 *@values* 사고법이 — 시장 사건 도메인에서 활용된 자리다.

`trigger_event` 속성은 *움직임의 트리거*를 자유 텍스트로 적는다. *NVIDIA GTC 키노트*, *분기 실적 발표*, *섹터 로테이션* 같은. 이상적으로는 이게 — 13장의 *technology_endorsement* 매듭과 직접 연결되어야 한다. 그 정교한 연결은 15장에서 짠다.

---

## 14.2 세 회사의 핵심 가격 움직임

이제 1편 회상에서 짚어둔 세 회사의 *결정적 가격 움직임*이 데이터에 들어간다. 이 데이터는 — *책의 분석을 위한 시연 데이터*다. 실제 분석에 쓰려면 *공식 거래 데이터, 한국거래소(KRX) 일별 시세, 각 회사의 공시 자료*를 원자료로 박아야 한다. 시연 자리에서의 수치는 *공개된 일자별 종가에서 추출한 것*이지만, *책의 시나리오에 맞춰 단순화된 형태*다.

쓰기 모드로 전환.

```typeql
transaction write optical_chain
```

```typeql
match
  $daehan_optical isa company, has name "대한광통신";
  $kwangjeon isa company, has name "광전자";
  $innoinstrument isa company, has name "이노인스트루먼트";

insert
  # === 대한광통신 — 대장주, 1년 이상 먼저 움직였다 ===
  $move1 (moving_company: $daehan_optical) isa significant_price_move,
    has move_date 2025-02-15,
    has move_type "first_significant_rise",
    has price_change_pct 35.0,
    has price_before 420.0,
    has price_after 567.0,
    has trigger_event "AI 데이터센터 광섬유 수요 본격 인식";
  
  $move2 (moving_company: $daehan_optical) isa significant_price_move,
    has move_date 2025-11-20,
    has move_type "breakout",
    has price_change_pct 25.0,
    has price_before 8000.0,
    has price_after 10000.0,
    has trigger_event "NVIDIA-Corning 광섬유 협력 발표";
  
  $move3 (moving_company: $daehan_optical) isa significant_price_move,
    has move_date 2026-04-12,
    has move_type "gap_up",
    has price_change_pct 25.3,
    has price_before 14000.0,
    has price_after 17770.0,
    has trigger_event "NVIDIA GTC 광반도체 발언";
  
  $move4 (moving_company: $daehan_optical) isa significant_price_move,
    has move_date 2026-05-10,
    has move_type "new_high",
    has price_change_pct 30.0,
    has price_before 18000.0,
    has price_after 22350.0,
    has trigger_event "AI 광섬유 수요 본격화";
  
  # === 광전자 — 4월 1일 갭 상승 ===
  $move5 (moving_company: $kwangjeon) isa significant_price_move,
    has move_date 2026-04-01,
    has move_type "gap_up",
    has price_change_pct 15.0,
    has price_before 2735.0,
    has price_after 3146.0,
    has trigger_event "NVIDIA GTC 광반도체 발언";
  
  $move6 (moving_company: $kwangjeon) isa significant_price_move,
    has move_date 2026-04-15,
    has move_type "new_high",
    has price_change_pct 550.0,
    has price_before 2735.0,
    has price_after 17780.0,
    has trigger_event "광반도체 테마 본격 확산";
  
  # === 이노인스트루먼트 — 4일 연속 상한가 ===
  $move7 (moving_company: $innoinstrument) isa significant_price_move,
    has move_date 2026-04-04,
    has move_type "consecutive_upper_limit",
    has price_change_pct 132.0,
    has price_before 880.0,
    has price_after 2045.0,
    has trigger_event "광반도체 테마 + 시공장비 후속 인식";
  
  $move8 (moving_company: $innoinstrument) isa significant_price_move,
    has move_date 2026-04-30,
    has move_type "new_high",
    has price_change_pct 1900.0,
    has price_before 252.0,
    has price_after 5080.0,
    has trigger_event "광통신 인프라 시공 수요 폭증 기대";

commit;
```

8개의 가격 움직임 매듭이 데이터에 들어갔다. 세 회사가 — *서로 다른 시점에, 서로 다른 강도로* 움직인 모양이 — 데이터 안에 명시적으로 박혔다.

여기서 가장 정직하게 짚어둘 자리. **위의 수치는 시연을 위한 단순화된 데이터다.** 실제 1년 동안의 가격 움직임은 — 여기 박힌 8개의 사건만이 아니라 *수십 개의 작은 움직임*으로 채워져 있다. 이 책의 분석은 *결정적 매듭만*에 집중하기 위해 simplification을 적용했다. 실제 시스템에서는 — 각 회사의 *일별 시세 전체*를 별도 저장소(시계열 DB)에 두고, *significant_price_move*는 *해석된 사건만* 추출해서 적는 *두 층 구조*가 자연스럽다.

데이터의 출처에 대해서도 한 가지 짚어둔다. 위 수치들은 *공개된 일별 종가 자료에서 추출한 시연 데이터*다. 정확한 분 단위 시세, 거래량, 호가 정보까지 박으려면 *별도의 시계열 데이터 소스*가 필요하다. 이 책의 분석은 *시점의 순서와 강도*에 의존하지, *정확한 가격*에 의존하지 않는다. 5편의 *시차 알파 함수*도 — 일자별 정밀도면 충분히 작동한다.

---

## 14.3 첫 시차 패턴 — 데이터가 1편 회상을 확인한다

데이터가 들어갔으니, 첫 번째 패턴을 확인한다. 1편 회상의 핵심 — *대한광통신이 1년 이상 먼저 움직였다*는 사실이 — 데이터 안에서 보이는지.

```typeql
transaction read optical_chain
```

```typeql
match
  $company isa company, has name $cname;
  $move (moving_company: $company) isa significant_price_move,
    has move_date $date,
    has move_type $type,
    has price_change_pct $change;

fetch
  $cname; $date; $type; $change;
```

답 (시점 순으로 정리):

```json
{ "cname": "대한광통신", "date": "2025-02-15", 
  "type": "first_significant_rise", "change": 35.0 }
{ "cname": "대한광통신", "date": "2025-11-20", 
  "type": "breakout", "change": 25.0 }
{ "cname": "광전자", "date": "2026-04-01", 
  "type": "gap_up", "change": 15.0 }
{ "cname": "이노인스트루먼트", "date": "2026-04-04", 
  "type": "consecutive_upper_limit", "change": 132.0 }
{ "cname": "대한광통신", "date": "2026-04-12", 
  "type": "gap_up", "change": 25.3 }
{ "cname": "광전자", "date": "2026-04-15", 
  "type": "new_high", "change": 550.0 }
{ "cname": "이노인스트루먼트", "date": "2026-04-30", 
  "type": "new_high", "change": 1900.0 }
{ "cname": "대한광통신", "date": "2026-05-10", 
  "type": "new_high", "change": 30.0 }
```

8개 사건이 시점 순으로 정리됐다. **시차 패턴이 데이터 안에서 명시적으로 보인다.**

대한광통신의 첫 의미 있는 상승이 — *2025년 2월 15일*. 광전자와 이노인스트루먼트의 첫 움직임이 시작된 *2026년 4월*보다 — *1년 2개월 먼저*다. 1편 회상에서 *대한광통신이 작년부터 움직였잖아요*라는 후배의 한마디 — 그 한마디가 데이터 안에서 *시점의 차이*로 자동으로 확인된다.

이노인스트루먼트의 *+1900%* 자리는 — 1편 1장에서 후배가 *1,000퍼센트 넘게*라고 말한 그 사건의 정확한 자릿수다. 술자리에서 던진 *넘게*라는 보수적 부사가 — 데이터에서는 *1900.0*이라는 수치로 적힌다. 252원에서 5,080원까지의 누적 변화율. 같은 한 사건의 두 표현이 한 책 안에서 만나는 자리. 회상의 어휘가 *데이터의 자릿수*로 정정되는 첫 자리이기도 하다.

그리고 한 가지 더. *광전자가 4월 1일, 이노인스트루먼트가 4월 4일*. 같은 광반도체 신호에 — 이노인스트루먼트가 *3일 늦게* 반응했다. 이 작은 시차도 데이터에 있다. *광전자 매수 시점 4월 1일에 이노인스트루먼트를 시스템이 후속 노드로 도출*할 수 있는 *데이터의 토대*가 — 여기 박혀 있다.

---

## 14.4 대장주 식별 함수의 기초

데이터가 시점 순으로 보였으니 — *대장주가 누구인가*를 함수로 식별한다. 가장 단순한 정의: *주어진 회사 집합 중에서, 가장 일찍 큰 움직임을 시작한 회사*.

```typeql
transaction schema optical_chain
```

```typeql
define
  fun first_significant_move_date($c: company) -> { date }:
    match
      ($c) isa significant_price_move, has move_date $date;
    return { $date };

commit;
```

이 함수는 *한 회사의 모든 significant_price_move 시점의 집합*을 반환한다. 가장 이른 시점만 뽑으려면 — TypeQL의 집계 함수 `min`을 사용한다.

```typeql
transaction read optical_chain
```

```typeql
match
  $beneficiary isa optical_component_maker, has name $bname;
  $date in first_significant_move_date($beneficiary);
  $earliest = min($date);

fetch
  $bname; $earliest;
```

이 모양으로는 — 각 회사별 *가장 이른 큰 움직임 시점*이 나온다. 책의 다음 장에서는 이 모양을 더 정교화한다. 지금은 *기초 패턴*만 짚어둔다.

대장주 식별의 본격 함수는 다음 모양으로 짤 수 있다.

```typeql
define
  fun leader_in_signal_path($technology: technology) -> { company }:
    match
      let $beneficiary in beneficiaries_of_technology($technology);
      ($beneficiary) isa significant_price_move, has move_date $date;
      $earliest = min($date);
      let $other_beneficiary in beneficiaries_of_technology($technology);
      ($other_beneficiary) isa significant_price_move, has move_date $other_date;
      $earliest <= $other_date;
    return { $beneficiary };

commit;
```

이 함수는 *주어진 기술과 관련된 모든 회사 중, 가장 일찍 움직임을 시작한 회사*를 반환한다. CPO 발언을 입력으로 던지면 — *대한광통신*이 답으로 나와야 한다. 1편 회상의 후배 한마디가 — 함수의 답으로 확인되는 모양이다.

이 함수의 진짜 본격 짜기는 — 15장에서 다룬다. 14장에서는 *기초만* 짚어둔다. *시점이 함수의 일급 인수가 된다*는 사실, *대장주 식별이 함수로 가능하다*는 사실이 박혀 있는 것으로 충분하다.

---

## 14.5 데이터 출처 — 회상이 어떻게 검증된 사실이 되는가

이 장에서 박은 데이터가 *회상에서 검증된 사실로 변환되는 과정*에는 — 명시적인 출처 정보가 필요하다. 이걸 짚어두지 않으면, 시스템이 *분석가의 직관*과 *공개된 사실*을 구분하지 못한다.

간단한 *출처 매듭*을 정의한다.

```typeql
transaction schema optical_chain
```

```typeql
define
  entity data_source,
    owns source_name @card(1..1),
    owns source_type @card(1..1),
    owns access_url @card(0..1);
  
  attribute source_name, value string;
  attribute source_type, value string 
    @values("public_filing", "exchange_data", "news_article", "analyst_report", "company_ir");
  attribute access_url, value string;
  
  relation cited_by,
    relates citing_event,
    relates cited_source;
  
  significant_price_move plays cited_by:citing_event;
  technology_endorsement plays cited_by:citing_event;
  data_source plays cited_by:cited_source;

commit;
```

여기서 — *significant_price_move*와 *technology_endorsement* 두 관계가 모두 *cited_by의 citing_event 자리에 들어갈 수 있게* 다중 plays가 설정됐다. 한 데이터 출처가 *여러 사건을 뒷받침하는* 모양도 자연스럽게 표현된다.

예시 출처 하나를 박는다.

```typeql
transaction write optical_chain
```

```typeql
match
  $event isa technology_endorsement, has announcement_date 2026-04-01;

insert
  $source isa data_source,
    has source_name "NVIDIA GTC 2026 Keynote Recording",
    has source_type "company_ir",
    has access_url "https://nvidia.com/gtc2026/keynote";
  
  (citing_event: $event, cited_source: $source) isa cited_by;

commit;
```

이 모양으로 — *발언 매듭*이 *출처 매듭*에 의해 뒷받침된다. 책의 후반 분석들이 *직관*이 아니라 *검증된 사실*에 기반한다는 사실이 — 데이터 안에서 *추적 가능*해진다.

실제 시스템에서는 — 모든 *significant_price_move*가 한국거래소 일별 시세 자료에, 모든 *technology_endorsement*가 *공식 발표 자료 또는 검증된 분석가 리포트*에, 모든 *supplies*가 *공식 IR 자료 또는 분기 보고서*에 — 출처가 박혀야 한다. 이게 책의 후반 분석이 *Disclosure Intelligence*(공시 인텔리전스)와 결합되는 자리다.

---

## 14.6 마무리 — 15장으로의 다리

이 장에서 한 일을 정리한다. *significant_price_move* 관계의 정의(다섯 종류의 move_type과 시점·가격 정보), 세 회사의 8개 핵심 가격 움직임이 데이터로, 시점 순 정리로 *시차 패턴*이 자동 확인, *대장주 식별 함수*의 기초, *데이터 출처 관계*의 정의.

1편 회상에서 후배가 한마디로 던졌던 *대장주가 먼저 가고 나머지가 따라온다*는 패턴이 — 이 장의 끝에서 *데이터 안의 명시적 사실*로 변환됐다. 대한광통신이 2025년 2월부터 움직였고, 광전자가 2026년 4월 1일에, 이노인스트루먼트가 4월 4일에. 시점의 차이가 *눈에 보이는* 자리에.

다음 장 — 15장 — 에서 *본격적인 시차 알파 분석*이 시작된다. 지금까지 짜둔 모든 데이터와 함수가 합쳐져, *대장주가 움직였고 후속이 아직 안 움직였을 때, 그 후속 노드를 자동으로 짚어내는 시스템*이 본격 작동한다. 1편 마지막에서 약속했던 그 시스템이 — 15장에서 처음으로 작동하는 모양으로 보인다.

회상이 사실로, 사실이 패턴으로, 패턴이 — 다음 장에서 — 시스템으로.
