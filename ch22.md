# 제22장. 정밀 시차 알파 — 시스템의 본격 작동

## 들어가며

21장에서 *가중치*가 시스템에 들어왔다. 다섯 광 부품 회사가 *서로 다른 무게*로 답에 정렬됐다. 그러나 — 가중치는 *후속 노드의 무게*만 다뤘다. *대장주가 얼마나 먼저 움직였는가*, *그 시차가 얼마나 큰가*, *움직임의 강도가 어땠는가* 같은 *시점과 강도의 정밀도*는 아직 답에 반영되지 않았다.

15장의 기초 `unmoved_followers`는 — *움직였느냐 안 움직였느냐*의 이진 판단만 했다. 분석 시점 *이전에 한 번이라도 큰 움직임이 있었으면 빼고*, *없으면 답에 포함*. 그러나 — 진짜 분석에서는 이 이진 판단으로는 부족하다. *대장주가 한 달 먼저 움직였는가, 1년 먼저 움직였는가*. *움직임의 강도가 +30%였는가, +500%였는가*. *그 강도가 신호의 본격성을 가리키는가, 그저 잡음인가*. 이 모든 정밀도가 — 함수의 답에 반영되어야 한다.

이 장이 그 정밀화의 자리다. 15장 기초 함수가 *시점 정밀도*와 *강도 정밀도*를 입어 — *진짜 시스템*에 한 발 더 가까워진다. 그리고 22장 끝에서 5편이 닫힌다. 6편 *뉴로심볼릭 클라이맥스*로의 다리가 거기에 박힌다.

---

## 22.1 기초 함수와 정교화 함수의 차이

15장의 함수를 다시 한 번 본다.

```typeql
fun unmoved_followers(
  $endorsement: technology_endorsement,
  $analysis_date: date
) -> { company }:
  match
    ($endorsement)(beneficiary_company: $candidate);
    not {
      ($candidate) isa significant_price_move, has move_date $any_date;
      $any_date < $analysis_date;
      $any_date >= 2025-01-01;
    };
  return { $candidate };
```

함수의 본질은 *부정 조건*(`not { ... }`)이었다. 그러나 그 부정의 조건이 — *어느 종류의 움직임도 무관*하게 *어떤 움직임이라도 있으면 빼는* 단순한 형태였다. *35% 짜리 작은 상승*도, *1900% 짜리 폭발*도 — 같은 *움직였다*로 처리됐다.

진짜 시스템은 — 이 단순함을 넘어선다. *어느 종류의 움직임이었는가*, *얼마나 컸는가*, *얼마나 최근이었는가*. 이 세 가지 정밀도가 들어가면 — 답은 *훨씬 더 풍부*해진다.

22장의 정교화 함수는 세 가지 입력 매개변수를 받는다.

- **`min_move_pct`** — 큰 움직임으로 간주할 최소 변화율 (기본 30%)
- **`exclude_move_types`** — 제외할 움직임 종류 (가령 *first_significant_rise*는 무시하지 않음)
- **`lookback_days`** — 분석 시점에서 며칠 이전까지 거슬러 볼 것인가 (기본 365일)

이 세 매개변수가 — *분석가의 정교화 도구*가 된다. 같은 함수를 호출하면서 *오늘은 보수적으로*, *내일은 공격적으로* 분석할 수 있다.

---

## 22.2 정밀 unmoved_followers — 본격 함수

스키마 모드로 전환.

```typeql
transaction schema optical_chain
```

```typeql
define
  # 정밀 시차 알파 함수
  fun precise_unmoved_followers(
    $endorsement: technology_endorsement,
    $analysis_date: date,
    $min_move_pct: double,
    $lookback_days: long
  ) -> { company }:
    match
      # 1. 발언의 beneficiary 회사들
      ($endorsement)(beneficiary_company: $candidate);
      
      # 2. 분석 시점부터 lookback_days만큼 거슬러 가는 임계 날짜
      $threshold_date = $analysis_date - $lookback_days;
      
      # 3. 후보 회사 중, threshold_date 이후 min_move_pct 이상의 큰 움직임이 없는 회사
      not {
        ($candidate) isa significant_price_move,
          has move_date $any_date,
          has price_change_pct $pct;
        $any_date < $analysis_date;
        $any_date >= $threshold_date;
        $pct >= $min_move_pct;
      };
    return { $candidate };

commit;
```

이전 함수의 두 가지 차이.

**차이 1.** `$threshold_date = $analysis_date - $lookback_days`로 *분석 기간의 시작점*이 매개변수로 결정된다. 15장에서는 *고정된 2025-01-01*이었던 자리.

**차이 2.** 부정 조건에 `$pct >= $min_move_pct`가 추가됐다. *최소 움직임 강도* 필터. 35% 상승 같은 *작은 움직임*은 — 매개변수에 따라 *무시*된다.

함수가 정의됐다. 이제 호출.

---

## 22.3 4월 1일 시점의 답이 진짜로 어떻게 보이는가

15장에서 호출했던 그 시점 — 2026-04-01 — 으로 다시 돌아간다. 이번에는 정밀 함수로.

```typeql
transaction read optical_chain
```

```typeql
match
  $endorsement isa technology_endorsement,
    has announcement_date 2026-04-01;
  
  # 보수적 호출 — 30% 이상의 큰 움직임이 있었으면 제외, 1년 거슬러 봄
  let $follower in precise_unmoved_followers(
    $endorsement, 
    2026-04-01,
    30.0,
    365
  );
  
  # 21장 가중치
  $cpo isa cpo_tech, has technology_name "Co-Packaged Optics";
  let $weight in company_weight_for_technology($follower, $cpo);
  
  $follower has name $fname;
  $follower has headquarters_country $country;

fetch
  $fname; $country; $weight;
```

답 (가중치 순):

```json
{ "fname": "Lumentum", "country": "USA", "weight": 0.90 }
{ "fname": "이노인스트루먼트", "country": "Korea", "weight": 0.75 }
{ "fname": "Coherent", "country": "USA", "weight": 0.55 }
{ "fname": "광전자", "country": "Korea", "weight": 0.30 }
```

네 회사. 21장과 *같은 답*이다. 21장에서 짚어둔 *군집 해석* — 상·중·하위 군으로 묶어 읽는 모양 — 이 이 답에도 그대로 적용된다. *0.90과 0.75의 정수 등수*보다 *상위 군(Lumentum·이노인스트루먼트)과 하위 군(광전자)의 경계*가 의미 있는 정보다. 대한광통신은 *2025-02-15에 35% 움직임* + *2025-11-20에 25% 움직임*. 둘 다 *임계점 30%* 근처에서, 첫 번째 움직임이 *조건을 충족*. 그래서 빠진다.

여기서 정밀 함수의 진짜 가치를 보려면 — 매개변수를 *바꿔서* 호출해야 한다. 두 가지 변형을 본다.

**변형 1 — 더 공격적: 70% 이상만 큰 움직임으로**

```typeql
match
  $endorsement isa technology_endorsement,
    has announcement_date 2026-04-01;
  
  let $follower in precise_unmoved_followers(
    $endorsement, 
    2026-04-01,
    70.0,    # 더 엄격한 임계점
    365
  );
  
  $follower has name $fname;

fetch
  $fname;
```

답:

```json
{ "fname": "광전자" }
{ "fname": "이노인스트루먼트" }
{ "fname": "대한광통신" }
{ "fname": "Lumentum" }
{ "fname": "Coherent" }
```

다섯 회사 *모두*. **대한광통신이 답에 다시 등장**한다. 대한광통신의 *가장 큰 과거 움직임*이 35%와 25%였는데, *70% 임계점*에서는 *둘 다 임계점 이하*가 된다. 시스템이 *대한광통신도 아직 본격 폭발하지 않았다*는 답을 준다.

이게 — 분석가의 *해석 권한*이 함수 안에 들어간 자리다. *35%를 큰 움직임으로 볼 것인가, 70%를 큰 움직임으로 볼 것인가*는 — 도메인 사고법의 영역이다. 함수의 코드는 변하지 않고, *임계점만 바꿔서* 다른 답을 받는다.

**변형 2 — 더 짧은 lookback: 90일만**

```typeql
match
  $endorsement isa technology_endorsement,
    has announcement_date 2026-04-01;
  
  let $follower in precise_unmoved_followers(
    $endorsement, 
    2026-04-01,
    30.0,
    90    # 3개월만 거슬러
  );
  
  $follower has name $fname;

fetch
  $fname;
```

답:

```json
{ "fname": "광전자" }
{ "fname": "이노인스트루먼트" }
{ "fname": "대한광통신" }
{ "fname": "Lumentum" }
{ "fname": "Coherent" }
```

다시 다섯 회사 모두. 대한광통신의 두 번째 움직임이 *2025-11-20*인데, 분석 시점 *2026-04-01*에서 *90일 거슬러*면 *2026-01-01부터*다. 그 안에는 대한광통신의 큰 움직임이 없다. 그래서 답에 다시 포함.

이게 *시점 정밀도의 가치*다. *시차의 시간 단위*가 분석가의 해석에 따라 달라진다. *3개월 시차*를 인식하는 분석가와 *1년 시차*를 인식하는 분석가가 — 같은 데이터에서 *다른 답*을 받는다. 함수는 — 두 해석을 모두 지원한다.

---

## 22.4 함수의 한 줄을 바꾸면 — 다른 사건도 같은 정밀도로

22.2의 함수는 *어느 사건에도 같은 모양으로 작동*한다. 15장 함수의 일반화 패턴이 — 정밀 함수에서도 그대로 유지된다.

16장의 HBM 채택 사건에 대해 — 정밀 함수를 호출한다.

```typeql
match
  $endorsement isa technology_endorsement,
    has announcement_date 2025-03-18;
  
  let $follower in precise_unmoved_followers(
    $endorsement, 
    2025-03-18,
    30.0,
    365
  );
  
  $follower has name $fname;

fetch
  $fname;
```

답:

```json
{ "fname": "SK하이닉스" }
{ "fname": "삼성전자" }
{ "fname": "Micron" }
```

세 회사. 16장의 답과 동일. *함수의 코드는 한 줄도 바뀌지 않았다*.

여기서 더 흥미로운 자리. *HBM 사건에 대한 가중치*를 만들려면 — 21장에서 짠 *광 부품 회사의 매출 구조*와 같은 종류의 데이터가 *HBM 메이커의 매출 구조*에도 박혀야 한다. *각 HBM 메이커의 매출 중 HBM 비중*. SK하이닉스가 70%, 삼성전자가 30% (기타 메모리 사업이 커서), Micron이 25% (DRAM 비중이 큼) 같은 식. 이런 데이터가 들어가면 — 같은 가중치 함수가 *HBM 메이커들에도 동일하게 작동*한다.

이게 — *시스템의 진짜 일반화*다. 한 번 짠 함수가 — *모든 종류의 사건과 모든 종류의 분류 가지*에 같은 모양으로 작동한다. 데이터만 자라면 — 함수는 변하지 않고 답이 풍부해진다.

---

## 22.5 5편 마무리 — 시스템의 발과 다리

5편(21~22장)에서 한 일을 한 번에 정리한다.

- **21장**: *business_segment_share* 관계로 회사·기술 단위 매출 비중 데이터. *company_weight_for_technology* 함수로 가중치 도출. 다섯 광 부품 회사가 *0.30~0.90 스펙트럼*으로 정렬.
- **22장**: *precise_unmoved_followers* 정밀 함수. *움직임 임계점*과 *분석 기간*이 매개변수화. 분석가의 해석이 *함수 호출 시점*에 반영.

3편이 *기초 시차 알파*였다면, 5편은 *정밀 시차 알파*다. 분석가의 도메인 사고법이 — 함수 매개변수로 *직접 표현*된다.

그러나 — 5편의 시스템에도 한 가지가 빠져 있다. **시스템은 *발언 매듭이 데이터에 박혀 있는 사건*에만 답한다.** 13장에서 *NVIDIA 광반도체 발언*을 데이터에 박았기 때문에, 15·22장 함수가 그 사건에 답할 수 있었다. 만약 *새로운 발언이 어제 일어났다*면, *그 발언을 누군가가 직접 데이터에 박아야* 시스템이 답할 수 있다.

매번 *발언 매듭을 손으로 박는 작업*은 — *시스템의 외부 의존*이다. 진짜 시스템은 *발언이 일어나는 순간 자동으로 그 발언을 매듭으로 변환*해야 한다. NVIDIA GTC 키노트가 새벽 두 시에 끝났을 때, 누군가가 매수 버튼 앞에서 *Co-Packaged Optics를 핵심으로 채택하겠다고 발표했다*는 사실을 데이터에 박는 작업 — *이걸 자동으로 할 수 있는가*. 그것이 시스템의 마지막 자리다.

---

## 22.6 6편으로의 다리 — 시스템의 머리

자동화의 핵심은 — *발언 자체를 이해하는 능력*이다. 그리고 *그 발언을 온톨로지의 자리에 맞게 변환하는 능력*. *Co-Packaged Optics*라는 단어가 키노트에서 등장했을 때, 시스템이 *우리의 기술 트리에서 cpo_tech entity가 그 단어에 대응한다*는 사실을 알아내야 한다. *NVIDIA의 다음 칩 세대 핵심*이라는 표현이 — *endorsement_strength가 strong이다*는 사실로 변환되어야 한다.

이건 — *언어 이해의 작업*이다. TypeDB나 다른 정형 시스템 단독으로는 — 불가능하다. 그러나 — *LLM*이 그 자리를 채운다. 자연어 발언을 받고, *온톨로지의 매듭 구조에 맞게 변환*. 이 결합이 — *뉴로심볼릭 (Neurosymbolic)*이라 불리는 그 자리다.

3편의 끝에서, 5편의 끝에서 — *내가 도구를 손에 익혔다*는 단계까지 왔다. 6편은 — *그 도구가 자기 스스로 도메인의 입력을 받아 작동하는* 자리다. 새벽 두 시의 키노트가 끝나면, 시스템이 *그 키노트를 듣고* *온톨로지의 매듭을 자동 추가하고*, 그 매듭에 *시차 알파 함수를 자동 호출하여* *후속 노드 후보를 분석가에게 답한다*.

분석가는 — *키노트가 끝난 직후*에 *시스템의 답을 받고*, *진입 결정을 한다*. 머릿속이 *어쩌다 외워둔 회사*에 의존하지 않는다. 그 새벽의 키보드 앞에서 — *시스템이 함께 일한다*.

1편 회상에서 약속한 *그 새벽의 풍경*이 — 6편에서 완성된다.

5편이 *시스템의 발과 다리*였다. 6편이 *시스템의 머리*다. 다음 편, 두 장. 책의 마지막 자리다.
