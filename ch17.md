# 제4편. 리스크 — 시스템이 부정 신호로 확장된다

## 편을 시작하며

3편의 끝에서 시스템은 *상승 신호*를 알아냈다. NVIDIA의 광반도체 발언이 입력으로 들어왔을 때, 그 발언이 영향을 미치는 회사 후보를 자동으로 도출했다. 광전자, 이노인스트루먼트, 대한광통신, Lumentum, Coherent. 시차 알파의 자리를 — 시스템이 짚어줬다.

그러나 — *상승의 자리만 짚어주는 시스템*은 절반의 시스템이다. 진짜 도구는 *하락의 자리*도 알아야 한다. 단일 공급원에 공급 차질이 생겼을 때, 그 충격이 어느 회사로 어떻게 전파되는가. NVIDIA의 한 핵심 부품이 *공급 중단의 위기*에 들어갔을 때, 그 신호가 어느 직간접 고객에 도달하는가. 이게 — 4편이 짓는 자리다.

4편은 4개 장으로 구성된다. **17장**에서 *단일 공급원의 매듭*이 자동으로 식별된다. **18장**에서 *부정 신호의 전파*가 그래프 위에서 펼쳐진다. **19장**에서는 *공급 병목*과 *기술 의존도*가 더 정교하게 다뤄지고, **20장**에서 4편의 시스템이 — 1편 회상과 한 번 더 만난다.

3편의 시스템이 *상승의 자리를 짚는 도구*였다면, 4편의 시스템은 *하락의 자리를 짚는 도구*다. 두 가지가 합쳐질 때 — 시스템은 *완전한 신호 분석 도구*가 된다. 시장은 상승만으로 움직이지 않는다.

---

# 제17장. 단일 공급원의 매듭 — 밸류체인의 결정적 자리

## 들어가며

12장에서 *공급 관계*를 데이터로 박을 때, 한 가지 속성을 함께 적었다. `is_sole_source`. *이 제품을 공급하는 유일한 공급자인가*. 그 시점에는 — 이 속성이 *어떻게 쓰이는지*까지 풀어내지 않았다. 단지 *데이터 모델에 박혀 있다*는 사실만 짚었다.

이 장에서 그 속성이 *진짜로 일하는 모양*이 보인다.

단일 공급원은 — 도메인의 *가장 결정적인 매듭*이다. 한 공급사가 문제에 들어가면, 그 위의 모든 회사가 직접적으로 영향을 받는다. 대체 공급사가 없기 때문이다. ASML이 EUV 노광 장비의 단일 공급사라는 사실은 — 단순한 회사 정보가 아니다. *글로벌 반도체 산업 전체가 한 회사에 의존한다*는 산업의 *구조적 사실*이다. 이런 매듭을 *시스템적으로* 찾아내는 일 — 그것이 17장의 자리다.

---

## 17.1 단일 공급원이 왜 결정적인가

먼저 도메인 사고법부터 짚어둔다. 단일 공급원이 *결정적*인 이유는 — 두 가지 비대칭에 있다.

**첫째, 충격 전파의 비대칭.** 일반적인 다중 공급사 관계에서는 — 한 공급사에 문제가 생겨도, 다른 공급사가 *부분적으로* 그 자리를 메운다. SK하이닉스의 HBM3E 공급에 차질이 생기면, 삼성전자와 Micron이 *후속 공급*으로 일부 메운다 (12장의 데이터에서 보았듯이). 충격이 *희석된다*. 그러나 — 단일 공급원에서는 그 희석이 *원리적으로 불가능하다*. ASML이 EUV를 못 만들면 — 다른 어디서도 그 자리가 메워지지 않는다. 충격이 *그대로 전달*된다.

**둘째, 협상력의 비대칭.** 단일 공급원은 — *고객이 공급사를 선택할 수 없다*. 가격을 협상할 자리도 좁다. 기술적 의존도가 높을수록, 단일 공급사의 *결정*이 산업 전체의 결정이 된다. ASML이 *어느 노드의 EUV에 우선순위를 둘 것인가*를 결정하면 — 그 결정이 TSMC, 삼성 파운드리, Intel, 그리고 그 위의 모든 칩 회사에 *간접적으로 전달*된다.

이 두 비대칭이 — 단일 공급원의 매듭을 *밸류체인 분석의 최우선 매듭*으로 만든다. 좋은 분석은, 좋은 시스템은 — 단일 공급원의 자리를 *즉시* 식별하고, 그 매듭이 *어느 회사들에 영향을 미치는지*를 자동으로 추적한다.

---

## 17.2 단일 공급원 매듭의 자동 식별

12장에서 박은 데이터에는 — 단일 공급원 표시가 있는 관계가 *두 개* 있었다. 다시 확인한다.

```typeql
transaction read optical_chain
```

```typeql
match
  $supply isa supplies,
    has is_sole_source true;
  
  (supplier: $supplier, customer: $customer, supplied_product: $product) isa supplies;
  $supply is $supply;
  
  $supplier has name $supplier_name;
  $customer has name $customer_name;
  $product has model_name $product_name;

fetch
  $supplier_name; $customer_name; $product_name;
```

답:

```json
{ "supplier_name": "TSMC", "customer_name": "NVIDIA", "product_name": "CoWoS-L Service" }
{ "supplier_name": "ASML", "customer_name": "TSMC", "product_name": "NXE:3800E" }
```

두 매듭. TSMC가 NVIDIA의 *유일한 첨단 패키징 공급사*, ASML이 TSMC의 *유일한 EUV 노광 장비 공급사*. 이 두 매듭이 — 현재 데이터베이스에서 *밸류체인의 가장 결정적 자리*다.

여기서 한정을 미리 짚어둔다. *유일한*이라는 표현은 *2024~2025년 시점, EUV가 필요한 최첨단 노드 기준의 사실상 단일성*을 가리킨다. 산업 분석가 일부는 Amkor·삼성 파운드리·Intel의 잠재 자리를 *부분 대체*로 분류한다. 단일성은 *시간에 종속된 속성*이고 — 17.5에서 그 종속성을 본격적으로 짚는다.

함수로 묶는다. *모든 단일 공급원 매듭*을 즉시 도출하는 함수.

```typeql
transaction schema optical_chain
```

```typeql
define
  fun all_sole_source_supplies() -> { supplies }:
    match
      $supply isa supplies,
        has is_sole_source true;
    return { $supply };

commit;
```

한 줄짜리 함수. 그러나 — 이 한 줄이 *밸류체인의 가장 큰 리스크 매듭들*을 즉시 답해준다. 새 데이터가 들어오면 — 함수는 *변하지 않고* 더 많은 매듭을 답한다. 시스템의 일관성이 — 이 자리에서도 작동한다.

---

## 17.3 ASML EUV — 글로벌 반도체 산업의 단일 매듭

위의 두 매듭 중 — *ASML → TSMC*의 자리가 특히 결정적이다. 왜인가.

TSMC는 NVIDIA의 단일 공급사이지만, *TSMC가 공급하는 시장 자체*가 NVIDIA 하나는 아니다. TSMC는 NVIDIA뿐 아니라 AMD, Apple, Qualcomm 등 *수많은 칩 회사*에 패키징과 제조를 공급한다. TSMC에 문제가 생기면 — *NVIDIA만의 문제*가 아니라 *글로벌 첨단 반도체 산업 전체의 문제*가 된다.

그리고 그 TSMC가 — *EUV 노광 장비*라는 단일 도구에 의존한다. EUV가 멈추면 *TSMC의 최첨단 노드 전체*가 멈춘다. 4nm, 3nm, 2nm — 모든 최신 노드의 칩 제조가 멈춘다. 그 영향이 NVIDIA뿐 아니라 *TSMC를 사용하는 모든 회사*로 *연쇄적으로* 전파된다.

이게 — *ASML의 단일 매듭이 글로벌 반도체 산업 전체의 단일 매듭이 되는* 메커니즘이다. ASML이 *직접* 공급하는 회사는 TSMC, 삼성 파운드리, Intel 등 소수의 파운드리들뿐이다. 그러나 그 영향은 *수백 개의 칩 회사*로 자동으로 전파된다. 8장에서 짠 *재귀 공급망 추적*이 — 이 자리에서 결정적으로 작동한다.

---

## 17.4 매듭의 무게 — 한 회사가 영향을 미치는 모든 직간접 고객

단일 공급원의 *진짜 무게*를 알려면 — 그 공급사가 *얼마나 많은 회사에 영향을 미치는지*를 알아야 한다. 직접 고객만이 아니라, 그 직접 고객의 고객, 또 그 고객의 고객까지. 8장의 `all_suppliers_of`의 *반대 방향* — *모든 직간접 고객*을 추적하는 함수가 필요하다.

```typeql
transaction schema optical_chain
```

```typeql
define
  # 한 공급사가 영향을 미치는 모든 직간접 고객
  fun all_customers_of($supplier: company) -> { company }:
    # 기저 — 직접 고객
    match
      ($supplier, $direct_customer, $product) isa supplies;
    return { $direct_customer };
    
    or
    
    # 재귀 — 중간 고객의 고객
    match
      ($supplier, $mid, $product) isa supplies;
      let $indirect_customer in all_customers_of($mid);
    return { $indirect_customer };

commit;
```

8장 패턴의 *반대 방향*이다. 공급사를 입력으로 받고, *그 공급사의 모든 직간접 고객*을 펼친다. 호출.

```typeql
transaction read optical_chain
```

```typeql
match
  $asml isa company, has name "ASML";
  let $customer in all_customers_of($asml);
  $customer has name $cname;

fetch
  $cname;
```

답:

```json
{ "cname": "TSMC" }
{ "cname": "NVIDIA" }
```

두 회사. *현재 데이터베이스에 있는 ASML의 모든 직간접 고객*. TSMC가 직접 고객이고, NVIDIA가 *TSMC를 통한 간접 고객*이다.

여기서 한 가지 정직한 짚음. 이 답은 — *현재 데이터베이스의 한계*다. 실제로 ASML의 영향 범위는 *훨씬 넓다*. AMD, Apple, Qualcomm, Broadcom, Intel, MediaTek 같은 *TSMC의 다른 고객들*도 ASML의 간접 고객이지만, 이 회사들이 데이터베이스에 *아직 들어 있지 않다*. 그리고 삼성 파운드리, Intel 같은 *ASML의 다른 직접 고객들*도 데이터베이스에 없다.

데이터의 한계가 — 분석의 한계가 된다. 9~14장에서 데이터를 *15개 핵심 회사*로 단순화한 결과가, 이 자리에서 보인다. 책의 후반에서 *데이터 규모를 확장*하면 — 같은 함수가 *훨씬 더 풍부한 답*을 준다. 함수는 변하지 않는다. 데이터만 자란다. 16장에서 짚었던 *시스템의 일관성*이 — 이 자리에서도 작동한다.

함수의 답을 결합해서 — *각 단일 공급원의 영향 범위*를 한 번에 본다.

```typeql
match
  $supply isa supplies,
    has is_sole_source true;
  
  (supplier: $supplier, customer: $direct_customer, supplied_product: $product) isa supplies;
  $supply is $supply;
  
  $supplier has name $supplier_name;
  $product has model_name $product_name;
  
  let $any_customer in all_customers_of($supplier);
  $any_customer has name $any_customer_name;

fetch
  $supplier_name; $product_name; $any_customer_name;
```

답:

```json
{ "supplier_name": "TSMC", "product_name": "CoWoS-L Service", "any_customer_name": "NVIDIA" }
{ "supplier_name": "ASML", "product_name": "NXE:3800E", "any_customer_name": "TSMC" }
{ "supplier_name": "ASML", "product_name": "NXE:3800E", "any_customer_name": "NVIDIA" }
```

세 행. *각 단일 공급원이 영향을 미치는 모든 직간접 고객*. TSMC가 단일 공급원으로서 NVIDIA에 영향을 미치고, ASML이 단일 공급원으로서 TSMC와 NVIDIA 모두에 영향을 미친다. ASML의 영향이 *TSMC를 통과해* NVIDIA까지 도달하는 모양이 — 데이터 안에서 보인다.

이게 — 단일 공급원 매듭의 *진짜 무게*가 보이는 자리다. 단순히 *한 회사가 단일 공급사다*가 아니라, *그 단일 공급사가 어디까지 영향을 미치는가*가 함께 답으로 나온다.

---

## 17.5 정직한 한계 — 단일 공급원 데이터의 신뢰도

여기까지의 분석이 강력하다고 해서 — *데이터의 한계*가 사라지는 것은 아니다. 정직하게 짚어둔다.

**첫째, `is_sole_source` 표시의 정확성.** 12장에서 박은 *TSMC가 NVIDIA의 단일 공급사*, *ASML이 TSMC의 단일 공급사*는 — *현재 시점의 사실*에 가깝지만, *2~3년 뒤에도 같을지*는 모른다. 삼성 파운드리가 첨단 노드에서 TSMC를 따라잡으면 — TSMC의 단일성이 약해진다. 중국이 EUV 자체 개발에 성공하면 — ASML의 단일성이 흔들린다. 단일성은 *시간에 따라 변하는 속성*이고, 데이터에 시점 정보가 함께 있어야 한다.

이걸 표현하려면 — `is_sole_source` 속성에 *기간*을 함께 박는 모양이 필요하다. *2024년부터 2028년까지 단일 공급원이었다*는 식. 실제 시스템에서는 이런 *시간 의존 속성*이 필수다. 이 책의 단순화된 모델에서는 생략됐지만, 본격 시스템에서는 반드시 필요한 자리다.

**둘째, *부분 단일성*의 처리.** 어떤 공급사는 *완전한 단일 공급사*가 아니라 *어느 노드/세대에서만 단일 공급사*인 경우가 있다. ASML의 EUV는 — *2024년 시점의 최첨단 노드(EUV가 필요한 3nm 이하)*에서는 단일 공급사지만, *5nm 이상의 노드*에서는 다른 옵션이 있다. 이런 *부분 단일성*을 표현하려면 — 더 정교한 데이터 모델이 필요하다.

**셋째, *비공식 단일성*.** 어떤 공급 관계는 — *법적으로는 다른 공급사가 가능하지만, 실질적으로 단일 공급원*인 경우가 있다. 가령 어떤 특수 부품에 *공식적인 후속 공급사*가 있지만, 그 회사의 생산 능력이 *너무 작아서 실질적으로는 한 회사만 공급할 수 있는* 상황. 이런 *실질적 단일성*은 데이터에 박기 까다롭다. 분석가의 *해석*이 들어가야 하기 때문이다.

이 세 가지 한계에도 불구하고 — 단일 공급원 분석의 *핵심 가치*는 흔들리지 않는다. *데이터에 표시된 단일 매듭*을 시스템이 즉시 식별하고, 그 영향 범위를 자동으로 추적하는 능력은 — 분석가의 머릿속이 *주기적으로 갱신해야 하는 정보*를 시스템에 *영구히 저장*한다.

---

## 17.6 18장으로의 다리

이 장에서 한 일을 정리한다. 단일 공급원의 도메인적 의미, `all_sole_source_supplies` 함수로 단일 매듭 자동 식별, `all_customers_of` 함수로 영향 범위 추적, 단일 매듭의 무게가 *직간접 고객 전체*로 펼쳐지는 모양, 그리고 정직한 한계.

여기까지가 — *어디에 단일 매듭이 있는가*에 대한 답이다. 그러나 — *그 매듭에 실제로 사건이 일어났을 때 어떻게 되는가*는 아직 다루지 않았다.

다음 장이 그 자리다. **부정 신호의 전파.** ASML EUV에 공급 차질이 생기는 가상 사건을 데이터에 박고, 그 신호가 어떻게 그래프 위로 전파되는지를 본다. 13장의 *상승 신호 발언*과 같은 모양으로 — *부정 사건* 관계가 정의되고, 그 사건이 *어느 회사들에 영향을 미치는지*가 자동으로 도출된다.

상승 신호와 부정 신호. 같은 그래프 위에서 — 같은 모양의 함수로 — 다른 방향으로 전파되는 모양. 시스템이 *대칭적이면서도 비대칭적*인 자리.
