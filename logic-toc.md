# TypeDB의 추론 — 함수가 사실을 짓는 자리

**부제**: 매칭에서 합성까지, 5장으로 천천히 짚는 *추론*의 본진. TypeDB가 다른 데이터베이스와 갈라지는 정확한 한 지점.

---

## 책의 자리

데이터베이스에 *적힌 사실*과, 데이터베이스가 *답할 수 있는 사실*은 같지 않다. 그 차이를 메우는 자리가 **추론(inference)**다.

이 책은 자매 책 두 권 — *광전자에서 시작된 한 권의 책*(회상), *TypeDB로 짓는 드론 군집비행 온톨로지*(기술 실전) — 에서 *암묵적으로 작동하던 추론*을 끄집어내, **다섯 장에 걸쳐 천천히** 본격적으로 짚는 책이다.

자매 책들이 *분류·관계·역할·시점*이라는 네 도구를 도메인에서 보였다면, 이 책은 그 네 도구가 **공통으로 서 있는 단 하나의 토대 — 추론**을 본다. TypeDB가 PostgreSQL·Neo4j·MongoDB와 자리를 달리하는 정확한 한 지점이 여기다.

> *데이터베이스가 답하는 것은 — 적힌 것에 멈추지 않는다. 적힌 것에서 도출되는 것까지다.*

이 한 줄을 다섯 장에 걸쳐 풀어내는 책이다.

---

## 이 책의 호흡

자매 책 두 권에 비해, 이 책은 **더 천천히 간다**.

- *드론 책*은 입문서이면서도 한 도메인 전체를 한 번에 짠다. 호흡이 빠르다.
- *광전자 책*은 회상이라 — 코드가 에세이의 곁가지에 있다.
- 이 책은 다르다. **개념 한 개를 한 장씩**. 각 장 끝에 *손에 잡힌 것*을 정리하고 — 다음 장에서 그 위에 한 켜씩 얹어 간다.

독자가 TypeDB를 **처음 만나는 사람**이라도 — 1장부터 5장까지 차례로 읽으면 *추론*이라는 단어가 *수사*가 아니라 *작동하는 도구*로 손에 들어오게 짰다.

---

## 책의 구성

```
1장. 매칭은 이미 추론이다
       — type hierarchy 위의 자동 일반화

2장. function의 기초
       — 사실을 변환하는 작은 단위

3장. 재귀가 추론이 되는 자리
       — 직접 관계에서 간접 관계로

4장. constraint
       — 사실을 강제하고 거부하는 자리

5장. 합성
       — 네 도구가 한 쿼리에서 동시에 작동하는 자리

부록
       A. TypeQL 추론 치트시트
       B. 학술 자료 — Datalog에서 PolyModel까지
       C. 자매 책 두 권과의 교차 색인
```

---

## 1장. 매칭은 이미 추론이다

> *추론은 무거운 단어처럼 들리지만, 사실 TypeDB의 매칭 한 줄 안에 이미 작동하고 있다.*

**무엇을 짚는가**
- *데이터·정보·지식*의 경계 — 같은 도메인을 세 가지 무게로 본다
- 매칭(`match`)이 왜 *추론*이라고 불릴 만한가 — 1차 추론의 의미
- `sub`가 만드는 자동 일반화 — *drone*을 묻지 않아도 *vehicle*을 물으면 잡힌다
- `plays`가 만드는 다형적 매칭 — 한 개체가 여러 자리를 동시에 차지하는 자리

**손에 잡히는 것**
- `entity X sub Y`의 상속 의미
- `match` 한 줄이 type hierarchy를 따라 자동으로 넓혀지는 모양
- *Liskov 치환 원칙*이 데이터베이스에 박힌 자리

**1장의 약속**
- 코드는 *짧고 친숙한 도메인*(도서관 장서, 동물 분류)에서만 짠다
- 학술 어휘는 *필요한 자리*에서만 한 줄씩
- 끝에 *◇ 정리* — 무엇이 손에 들어왔는가

---

## 2장. function의 기초 — 사실을 변환하는 작은 단위

> *데이터에 적힌 것을 — 묻기 좋은 모양으로 한 번 더 짓는 자리.*

**무엇을 짚는가**
- TypeDB 3.0이 *rule*을 *function*으로 흡수한 결정 — 왜?
- 단일 반환 vs 집합 반환
- function의 *문법 해부* — `fun ... -> { ... }: match ... return { ... };`
- 합성성(compositionality) — 한 function의 출력이 다른 function의 입력이 되는 자리
- *순수 함수*로서의 function — 부수효과 없음의 의미

**손에 잡히는 것**
- `manager_of($p)` — 단일 반환의 가장 작은 예
- `direct_reports_of($p)` — 집합 반환
- 함수 합성 — `match ... let $x in fn(...) ...`

**2장의 약속**
- 1장의 *매칭*이 *재사용 가능한 이름*을 가지게 되는 자리
- *함수형 프로그래밍*에 무경험이어도 막히지 않게 — *그릇과 내용물*의 비유로 풀이

---

## 3장. 재귀가 추론이 되는 자리

> *직접 관계에서 간접 관계로 — 적힌 적 없는 사실이 시스템 안에서 도출되는 자리.*

**무엇을 짚는가**
- *A가 B의 부모, B가 C의 부모 → A는 C의 조부모*가 자동으로 도출되는 모양
- 재귀 함수의 형태 — *base case*와 *recursive step*
- `or` 분기로 재귀를 짜는 TypeDB의 패턴
- **fixpoint 의미론** — 재귀가 *언제 멈추는가*에 대한 형식적 답 (Knaster-Tarski)
- 종료성·성능·무한 재귀의 위험과 방지

**손에 잡히는 것**
- `all_subordinates_of($p)` — 한 사람의 모든 직간접 부하
- `all_dependencies_of($pkg)` — 한 패키지의 모든 의존 패키지
- *재귀의 자리*를 도메인에서 식별하는 사고법

**3장의 약속**
- 수학을 두려워하지 않게 — *Knaster-Tarski*는 한 단락으로, *그 의미*는 그림으로
- *Datalog*의 50년 역사를 *세 문단으로* 압축
- 끝에 *◇ 정리* — 1·2·3장이 누적된 한 그림

---

## 4장. constraint — 사실을 강제하고 거부하는 자리

> *추론의 다른 얼굴. 무엇이 사실이 될 수 있는가의 약속.*

**무엇을 짚는가**
- 추론이 *사실을 만드는 자리*라면, constraint는 *사실의 자격을 정하는 자리*
- `@card` — 카디널리티가 만드는 **부정 추론** ("정확히 한 명의 부모"는 *두 번째를 거부한다*)
- `@values` — 허용된 값 목록
- `@range` — 수치 범위
- `@regex` — 문자열 패턴
- 스키마 검증이 *추론의 무결성*을 지키는 자리

**손에 잡히는 것**
- 잘못된 데이터를 *데이터베이스가 거부*하는 시연
- *Open-world*(OWA) vs *Closed-world*(CWA) — TypeDB의 입장
- *건강한 도메인*을 짜는 constraint 패턴

**4장의 약속**
- *제약*이 *제한*이 아니라 *약속*임을 — 동호회 회칙의 비유로
- *정직한 거부*가 *조용한 오염*보다 낫다는 입장

---

## 5장. 합성 — 네 도구가 한 쿼리에서 동시에 작동하는 자리

> *1장의 매칭, 2장의 함수, 3장의 재귀, 4장의 constraint가 — 한 쿼리 안에서 한꺼번에 작동하는 자리.*

**무엇을 짚는가**
- 자매 책 *드론 책 8장의 손실 시나리오*를 — **추론 관점에서 다시 본다**
- 한 쿼리 안에서:
  - `sub`이 만드는 다형성
  - `function`이 만드는 변환
  - `recursion`이 만드는 그래프 탐색
  - `@card`가 만드는 약속
- 이 네 가지가 *동시에* 작동하는 모양을 코드로 본다
- **추론의 한계**
  - Closed-world assumption — *적히지 않은 것은 거짓인가?*
  - 결정 가능성 — *언제 시스템이 답을 못 내는가?*
  - 성능 — *재귀가 깊어질 때*
- **추론과 LLM** — 결합의 미래
  - 자연어 → 매듭 변환
  - 추론 엔진 + 확률적 추론의 결합

**손에 잡히는 것**
- 추론을 *수사*가 아닌 *도구*로 손에 잡는 자리
- *TypeDB가 다른 데이터베이스와 갈라지는 정확한 한 지점*

**5장의 약속**
- 시리즈의 *완결편* — 세 권이 어떻게 한 그림이 되는가
- *다음 발걸음*의 안내 — 부록 B의 학술 자료와 함께

---

## 부록

### A. TypeQL 추론 치트시트

- `sub`, `plays`, `relates`, `owns` — 매칭 관련
- `fun`, `match`, `return`, `let`, `in`, `or` — 함수 관련
- `@card`, `@range`, `@values`, `@regex` — 제약 관련
- 자주 막히는 다섯 가지 패턴과 해결

### B. 학술 자료 — Datalog에서 PolyModel까지

- B.1 Robinson 1965 — 해소 원리 (Resolution)
- B.2 Kowalski 1974 — Horn clause 프로그래밍
- B.3 Knaster-Tarski 정리 — fixpoint의 형식 의미
- B.4 Davidson 1967 — 사건 의미론
- B.5 Gruber 1993 — 온톨로지의 표준 정의
- B.6 OWL 2 profiles — EL / QL / RL
- B.7 TypeDB 3.0 — PolyModel 백서

### C. 자매 책 두 권과의 교차 색인

- C.1 *광전자 책*의 어디서 추론이 작동하는가 — 24장의 추론 사용 표
- C.2 *드론 책*의 어디서 추론이 작동하는가 — 8장의 추론 사용 표
- C.3 *이 책 5장*과 *드론 책 7·8장*의 합본 읽기 안내

---

## 책의 분량

- 1장 ~ 5장: 각 ~7,000자 = ~35,000자
- 부록 A·B·C: ~5,000자
- **합계**: ~40,000자 (단행본 1권 분량의 약 1/3 — 시리즈의 *심장 챕터* 한 권)

자매 책 두 권이 *도메인의 두께*로 채워졌다면, 이 책은 **개념의 깊이**로 채워진다. 짧지만, 시리즈에서 가장 자주 다시 펴게 될 한 권을 의도한다.

---

## 시리즈 안에서의 자리

```
┌─────────────────────────────────────────────────────┐
│  광전자에서 시작된 한 권의 책 (회상 · 24장)         │
│  → 분석가의 시점에서 본 PolyModel의 작동            │
├─────────────────────────────────────────────────────┤
│  TypeDB로 짓는 드론 군집비행 온톨로지 (기술 · 9장) │
│  → 한 도메인의 전체 사이클을 빠르게                 │
├─────────────────────────────────────────────────────┤
│  TypeDB의 추론 (이 책 · 5장)                        │
│  → 자매 책 두 권의 공통 토대를 깊게                 │
└─────────────────────────────────────────────────────┘
```

세 권 중 가장 짧지만 — *가장 자주 회수되는 책*. 자매 책에서 *왜 이렇게 짜는가*가 막힐 때, 이 책의 한 장이 답한다.

---

**문서 정보**
- 파일명: `logic-toc.md`
- 위치: 자매 책과 같은 디렉터리
- 다음 작업: `logic-ch01.md`부터 본문 집필
- 상단 탭: `index.html`(회상) · `technical.html`(드론) · `logic.html`(이 책)
