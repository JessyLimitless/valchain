# 제11장. 제품과 기술의 분류

## 들어가며

10장에서 회사 15개가 데이터로 들어갔다. 그러나 — *회사*만으로는 그림이 완성되지 않는다. *어느 회사가 무엇을 만드는가*가 빠져 있다. NVIDIA가 만드는 H100과 SK하이닉스가 만드는 HBM3E는 *완전히 다른 종류*의 제품인데, 데이터 모델 안에서 그 차이가 표현되지 않으면 — 분석은 거기서 멈춘다.

이 장은 *제품*과 *기술*이라는 두 가지 entity 트리를 짜는 자리다. 그리고 회사와 제품을 잇는 관계(*만든다*), 제품과 기술을 잇는 관계(*기술을 쓴다*)를 정의한다. 이게 끝나면 — 다음 장의 *공급 관계*가 자연스럽게 그 위에 얹힌다.

이 장의 모든 코드는 10장의 스키마 위에서 작동한다. 10장 끝의 데이터(15개 회사)는 그대로 유지된 채, 그 위에 새 정의가 *추가*된다.

---

## 11.1 제품 entity 트리

제품의 분류는 *9장 6개 층의 그림*을 따라 짠다. 한 회사가 만드는 것은 — 그 회사가 속한 층의 성격과 묶여 있다. L1의 회사는 *AI 칩*을, L3의 회사는 *HBM*을, L4의 회사는 *후공정 장비*를 만든다.

스키마 모드에서 정의한다.

```typeql
transaction schema optical_chain
```

```typeql
define
  # === 제품 최상위 ===
  entity product,
    owns model_name @card(1..1),
    owns release_year @card(0..1);
  
  attribute model_name, value string;
  attribute release_year, value long @range(2000..2100);
  
  # === L1 제품: AI 칩 ===
  entity ai_chip sub product;
  entity gpu sub ai_chip;
  entity custom_ai_asic sub ai_chip;
  
  # === L2 제품: 패키징·인터커넥트 ===
  entity packaging_service sub product;
  entity interconnect_technology sub product;
  
  # === L3 제품: 메모리 ===
  entity memory_chip sub product;
  entity hbm sub memory_chip;
  entity ddr_dram sub memory_chip;
  entity nand_flash sub memory_chip;
  
  # === L4 제품: 후공정 장비 ===
  entity backend_equipment sub product;
  entity tc_bonder sub backend_equipment;
  entity test_socket sub backend_equipment;
  
  # === L5 제품: 전공정 장비·소재 ===
  entity frontend_equipment sub product;
  entity euv_lithography_system sub frontend_equipment;
  entity etching_equipment sub frontend_equipment;
  entity deposition_equipment sub frontend_equipment;
  
  entity semiconductor_material sub product;
  entity photoresist sub semiconductor_material;
  entity etchant sub semiconductor_material;
  
  # === L6 제품: 광통신·인접 ===
  entity optical_component sub product;
  entity optical_semiconductor sub optical_component;
  entity optical_fiber_product sub optical_component;
  entity optical_installation_equipment sub optical_component;
  entity cpo_module sub optical_component;

commit;
```

제품 트리가 6개 층의 회사 트리와 *대응하는 모양*으로 짜였다. L1 회사들이 L1 제품을 만들고, L6 회사들이 L6 제품을 만드는 식. 이 대응이 — 11.3에서 *회사가 제품을 만든다*는 관계를 정의할 때 자연스럽게 작동한다.

분류의 깊이는 *질문에 답할 수 있을 만큼*이다 (7장 회상). 가령 `hbm` 안에서 *HBM3, HBM3E, HBM4*로 더 쪼갤 수도 있지만 — 이 책의 분석에서는 *HBM 전체*를 한 단위로 보는 것이 충분하다. 더 정밀한 분석이 필요해지면 — 그때 가지를 더 쪼개면 된다.

---

## 11.2 기술 entity 트리

제품이 *물리적 산출물*이라면, *기술*은 *그 산출물 안에 들어 있는 발상*이다. 한 제품이 *어떤 기술을 사용하는지*가 — 신호 전파의 결정적 자리다.

NVIDIA의 H100이라는 제품은 — *CoWoS-L 패키징*, *HBM3 메모리 통합*, *NVLink 인터커넥트* 같은 *여러 기술*을 함께 사용한다. 같은 회사가 다음 세대 칩에서 *광 인터커넥트(CPO)*를 채택하기 시작하면 — 그 기술이 새로 도입되는 신호 자체가 *L6 광통신 회사*들에 도달한다. 이게 1편 회상의 핵심 사건이었다.

```typeql
define
  # === 기술 최상위 ===
  entity technology,
    owns technology_name @card(1..1),
    owns first_commercial_use_year @card(0..1);
  
  attribute technology_name, value string;
  attribute first_commercial_use_year, value long @range(2000..2100);
  
  # === 패키징 기술 ===
  entity packaging_technology sub technology;
  entity cowos_technology sub packaging_technology;
  entity foveros_technology sub packaging_technology;
  
  # === 인터커넥트 기술 ===
  entity interconnect_tech sub technology;
  entity electrical_interconnect sub interconnect_tech;
  entity optical_interconnect sub interconnect_tech;
  entity cpo_tech sub optical_interconnect;
  entity lpo_tech sub optical_interconnect;
  
  # === 메모리 기술 ===
  entity memory_technology sub technology;
  entity hbm_technology sub memory_technology;
  
  # === 노광·증착 기술 ===
  entity manufacturing_technology sub technology;
  entity euv_lithography sub manufacturing_technology;

commit;
```

기술 트리가 짜였다. 여기서 결정적인 자리는 *광 인터커넥트* 가지다. `optical_interconnect`라는 상위 아래에 `cpo_tech`와 `lpo_tech`가 들어가고, 그 위에 *전기 인터커넥트*(`electrical_interconnect`)와 같은 자리에 있다. *광 인터커넥트가 전기 인터커넥트를 대체하는* 큰 흐름이 — 분류 트리 안에 *형제 가지*로 박혀 있다.

1편 회상의 *NVIDIA 광반도체 발언*은 — 이 분류 트리 위에서 보면 — *기존 전기 인터커넥트에서 광 인터커넥트로의 무게중심 이동을 알린 사건*이다. 그 발언이 신호로 변환되어 L6 광통신 회사로 도달한 모양이 — 다음 절들에서 데이터로 짜인다.

이제 기술 데이터를 넣는다.

```typeql
transaction write optical_chain
```

```typeql
insert
  $cowos_l isa cowos_technology,
    has technology_name "CoWoS-L",
    has first_commercial_use_year 2022;
  
  $hbm3e_tech isa hbm_technology,
    has technology_name "HBM3E",
    has first_commercial_use_year 2024;
  
  $euv_tech isa euv_lithography,
    has technology_name "EUV Lithography",
    has first_commercial_use_year 2019;
  
  $cpo_tech isa cpo_tech,
    has technology_name "Co-Packaged Optics",
    has first_commercial_use_year 2025;
  
  $lpo_tech isa lpo_tech,
    has technology_name "Linear-drive Pluggable Optics",
    has first_commercial_use_year 2024;

commit;
```

다섯 가지 기술이 들어갔다. 각각의 *상용화 시점*이 함께 박혔다 — 이게 14장 *시점과 가격 움직임*에서 결정적 자리가 된다. 새 기술의 상용화 시점이, 시장의 *신호가 시작된 시점*과 거의 일치하는 경우가 많기 때문이다.

---

## 11.3 회사가 제품을 만든다 — `manufactures` 관계

회사와 제품이 분류로 짜였으니, 이제 *어느 회사가 어느 제품을 만드는가*를 관계로 잇는다.

```typeql
define
  relation manufactures,
    relates maker @card(1..1),
    relates manufactured_product @card(1..1),
    owns started_year @card(0..1),
    owns is_flagship_product @card(0..1);
  
  company plays manufactures:maker;
  product plays manufactures:manufactured_product;
  
  attribute started_year, value long @range(2000..2100);
  attribute is_flagship_product, value boolean;

commit;
```

`manufactures` 관계가 정의됐다. *maker* 자리에 회사가, *manufactured_product* 자리에 제품이 들어간다. 그리고 두 가지 속성 — 만들기 시작한 시점, 그리고 *대표 제품 여부* — 이 함께 적힌다.

이제 핵심 회사들이 만드는 제품을 입력한다. 먼저 제품 인스턴스를 넣고, 그 다음 관계를 짠다.

```typeql
transaction write optical_chain
```

```typeql
insert
  # === L1 제품 ===
  $h100 isa gpu,
    has model_name "H100",
    has release_year 2023;
  
  $h200 isa gpu,
    has model_name "H200",
    has release_year 2024;
  
  $b200 isa gpu,
    has model_name "B200",
    has release_year 2025;
  
  # === L2 제품 ===
  $cowos_service isa packaging_service,
    has model_name "CoWoS-L Service";
  
  # === L3 제품 ===
  $hbm3e_product isa hbm,
    has model_name "HBM3E";
  
  $hbm4_product isa hbm,
    has model_name "HBM4",
    has release_year 2025;
  
  # === L4 제품 ===
  $tc_bonder_product isa tc_bonder,
    has model_name "TC Bonder";
  
  # === L5 제품 ===
  $euv_system isa euv_lithography_system,
    has model_name "NXE:3800E";
  
  $photoresist_product isa photoresist,
    has model_name "EUV Photoresist";
  
  # === L6 제품 ===
  $optical_semi isa optical_semiconductor,
    has model_name "Optical Semiconductor Components";
  
  $optical_fiber_p isa optical_fiber_product,
    has model_name "Optical Fiber Cable";
  
  $fusion_splicer isa optical_installation_equipment,
    has model_name "Optical Fiber Fusion Splicer";

commit;
```

12개의 제품 인스턴스가 들어갔다. 이제 이 제품들을 *만드는 회사*들과 묶는다.

```typeql
match
  # 회사 변수 바인딩
  $nvda isa company, has name "NVIDIA";
  $tsmc isa company, has name "TSMC";
  $hynix isa company, has name "SK하이닉스";
  $samsung isa company, has name "삼성전자";
  $hanmi isa company, has name "한미반도체";
  $asml isa company, has name "ASML";
  $dongjin isa company, has name "동진쎄미켐";
  $kwangjeon isa company, has name "광전자";
  $daehan_optical isa company, has name "대한광통신";
  $innoinstrument isa company, has name "이노인스트루먼트";
  
  # 제품 변수 바인딩
  $h100 isa gpu, has model_name "H100";
  $h200 isa gpu, has model_name "H200";
  $b200 isa gpu, has model_name "B200";
  $cowos_service isa packaging_service, has model_name "CoWoS-L Service";
  $hbm3e_product isa hbm, has model_name "HBM3E";
  $hbm4_product isa hbm, has model_name "HBM4";
  $tc_bonder_product isa tc_bonder, has model_name "TC Bonder";
  $euv_system isa euv_lithography_system, has model_name "NXE:3800E";
  $photoresist_product isa photoresist, has model_name "EUV Photoresist";
  $optical_semi isa optical_semiconductor, has model_name "Optical Semiconductor Components";
  $optical_fiber_p isa optical_fiber_product, has model_name "Optical Fiber Cable";
  $fusion_splicer isa optical_installation_equipment, has model_name "Optical Fiber Fusion Splicer";

insert
  # NVIDIA — GPU 세 종
  (maker: $nvda, manufactured_product: $h100) isa manufactures,
    has started_year 2023, has is_flagship_product true;
  (maker: $nvda, manufactured_product: $h200) isa manufactures,
    has started_year 2024, has is_flagship_product true;
  (maker: $nvda, manufactured_product: $b200) isa manufactures,
    has started_year 2025, has is_flagship_product true;
  
  # TSMC — 패키징
  (maker: $tsmc, manufactured_product: $cowos_service) isa manufactures,
    has started_year 2022, has is_flagship_product true;
  
  # SK하이닉스 — HBM
  (maker: $hynix, manufactured_product: $hbm3e_product) isa manufactures,
    has started_year 2024, has is_flagship_product true;
  (maker: $hynix, manufactured_product: $hbm4_product) isa manufactures,
    has started_year 2025, has is_flagship_product true;
  
  # 삼성전자 — HBM
  (maker: $samsung, manufactured_product: $hbm3e_product) isa manufactures,
    has started_year 2024;
  
  # 한미반도체 — TC Bonder
  (maker: $hanmi, manufactured_product: $tc_bonder_product) isa manufactures,
    has started_year 2010, has is_flagship_product true;
  
  # ASML — EUV
  (maker: $asml, manufactured_product: $euv_system) isa manufactures,
    has started_year 2019, has is_flagship_product true;
  
  # 동진쎄미켐 — 포토레지스트
  (maker: $dongjin, manufactured_product: $photoresist_product) isa manufactures,
    has started_year 2015;
  
  # 광전자 — 광반도체
  (maker: $kwangjeon, manufactured_product: $optical_semi) isa manufactures,
    has started_year 1976, has is_flagship_product true;
  
  # 대한광통신 — 광섬유
  (maker: $daehan_optical, manufactured_product: $optical_fiber_p) isa manufactures,
    has started_year 1972, has is_flagship_product true;
  
  # 이노인스트루먼트 — 광섬유 융착접속기
  (maker: $innoinstrument, manufactured_product: $fusion_splicer) isa manufactures,
    has started_year 2007, has is_flagship_product true;

commit;
```

여기서 한 가지 짚어둘 자리가 있다. 한 회사가 *여러 제품*을 만드는 경우 — NVIDIA가 H100, H200, B200을 모두 만들고, SK하이닉스가 HBM3E와 HBM4를 모두 만든다 — 가 자연스럽게 표현된다. 그리고 *한 제품을 여러 회사가 함께 만드는 경우* — HBM3E를 SK하이닉스와 삼성전자가 함께 만드는 모양 — 도 같은 데이터 모델 안에 들어간다.

이게 *다대다 관계*가 자연스럽게 표현되는 모양이다. RDB라면 별도의 매핑 테이블이 필요했을 정보가 — TypeDB에서는 *manufactures 관계 자체*가 그 매핑이다.

---

## 11.4 제품이 기술을 사용한다 — `uses_technology` 관계

제품과 기술을 잇는 관계는 또 다른 자리다.

```typeql
define
  relation uses_technology,
    relates user_product @card(1..1),
    relates used_technology @card(1..1),
    owns is_critical @card(0..1);
  
  product plays uses_technology:user_product;
  technology plays uses_technology:used_technology;
  
  attribute is_critical, value boolean;

commit;
```

`is_critical` 속성은 — *그 기술이 제품의 핵심 기술인지*를 표시한다. NVIDIA H100에서 *CoWoS-L 패키징*은 핵심 기술이지만, 다른 부수적 기술들은 비핵심으로 두는 식이다. 이 표시가 — 신호 전파 분석에서 *어떤 채택이 진짜 신호인가*를 가르는 자리가 된다.

데이터를 입력한다.

```typeql
match
  $h100 isa gpu, has model_name "H100";
  $h200 isa gpu, has model_name "H200";
  $b200 isa gpu, has model_name "B200";
  $cowos_service isa packaging_service, has model_name "CoWoS-L Service";
  $hbm3e_product isa hbm, has model_name "HBM3E";
  
  $cowos_l isa cowos_technology, has technology_name "CoWoS-L";
  $hbm3e_tech isa hbm_technology, has technology_name "HBM3E";
  $euv_tech isa euv_lithography, has technology_name "EUV Lithography";
  $cpo_tech_inst isa cpo_tech, has technology_name "Co-Packaged Optics";

insert
  # NVIDIA H100 — CoWoS-L 핵심 기술
  (user_product: $h100, used_technology: $cowos_l) isa uses_technology,
    has is_critical true;
  
  # NVIDIA H200 — CoWoS-L + HBM3E 모두 핵심
  (user_product: $h200, used_technology: $cowos_l) isa uses_technology,
    has is_critical true;
  (user_product: $h200, used_technology: $hbm3e_tech) isa uses_technology,
    has is_critical true;
  
  # NVIDIA B200 — CoWoS-L + HBM3E + CPO
  (user_product: $b200, used_technology: $cowos_l) isa uses_technology,
    has is_critical true;
  (user_product: $b200, used_technology: $hbm3e_tech) isa uses_technology,
    has is_critical true;
  (user_product: $b200, used_technology: $cpo_tech_inst) isa uses_technology,
    has is_critical true;
  
  # TSMC CoWoS-L 서비스 — EUV 기술 사용
  (user_product: $cowos_service, used_technology: $euv_tech) isa uses_technology,
    has is_critical true;
  
  # SK하이닉스 HBM3E — HBM3E 기술 사용
  (user_product: $hbm3e_product, used_technology: $hbm3e_tech) isa uses_technology,
    has is_critical true;

commit;
```

8개의 *제품-기술* 매듭이 짜였다. 여기서 가장 결정적인 자리는 — B200의 세 번째 줄, *NVIDIA B200이 CPO를 핵심 기술로 채택한다*는 사실이다.

이게 1편 회상의 *NVIDIA 광반도체 발언*이 데이터로 박힌 모양이다. 발언만 적혀 있는 게 아니라 — *어느 제품이 그 기술을 핵심으로 사용하는지*가 함께 적혀 있다. 13장에서 본격적으로 *기술 채택 발언* 관계가 들어갈 때, 이 매듭이 그 발언의 *근거*가 된다.

---

## 11.5 첫 질문들 — 분류와 관계가 함께 작동하는 모양

이제 데이터 위에 첫 질문을 던진다. 10장에서는 *분류만으로* 답을 받았는데, 이 장에서는 *분류 + 관계*가 함께 작동한다.

### 질문 1 — HBM을 만드는 모든 회사

```typeql
transaction read optical_chain
```

```typeql
match
  $maker isa company, has name $name;
  $hbm_product isa hbm, has model_name $product_name;
  (maker: $maker, manufactured_product: $hbm_product) isa manufactures;
fetch
  $name; $product_name;
```

답:

```json
{ "name": "SK하이닉스", "product_name": "HBM3E" }
{ "name": "SK하이닉스", "product_name": "HBM4" }
{ "name": "삼성전자", "product_name": "HBM3E" }
```

세 행이 답으로 나왔다. 두 회사가 HBM 시장을 *공유*하면서, SK하이닉스가 HBM4까지 한 발 앞섰다는 사실이 *데이터 안에서* 보인다.

### 질문 2 — CPO 기술을 핵심으로 채택한 제품

NVIDIA의 광반도체 채택이 *어느 제품에 박혀 있는지*.

```typeql
match
  $cpo isa cpo_tech, has technology_name "Co-Packaged Optics";
  $product isa product, has model_name $product_name;
  (user_product: $product, used_technology: $cpo) isa uses_technology,
    has is_critical true;
  
  $maker isa company, has name $maker_name;
  (maker: $maker, manufactured_product: $product) isa manufactures;

fetch
  $product_name; $maker_name;
```

답:

```json
{ "product_name": "B200", "maker_name": "NVIDIA" }
```

NVIDIA B200이 *CPO를 핵심 기술로 채택한 유일한 제품*으로 답에 나왔다. *이 한 사건이 광통신 신호의 출발점이다*. 책의 후반 분석들이 — 이 한 매듭에서 시작한다.

### 질문 3 — 광 부품을 만드는 한국 회사

1편 회상의 세 회사를 *다른 각도*에서 다시 찾는다. 이번에는 *광 부품을 만드는* 한국 회사라는 조건으로.

```typeql
match
  $maker isa company,
    has name $name,
    has headquarters_country "Korea";
  $optical isa optical_component, has model_name $product_name;
  (maker: $maker, manufactured_product: $optical) isa manufactures;
fetch
  $name; $product_name;
```

답:

```json
{ "name": "광전자", "product_name": "Optical Semiconductor Components" }
{ "name": "대한광통신", "product_name": "Optical Fiber Cable" }
{ "name": "이노인스트루먼트", "product_name": "Optical Fiber Fusion Splicer" }
```

세 회사. 1편 회상의 정확히 그 세 회사가 — 이번에는 *제품 분류*를 기준으로 — 다시 답에 나왔다. 같은 답이 *서로 다른 질문*에서 나온다는 사실이 — 데이터 모델이 *일관되게* 짜여 있다는 신호다.

---

## 11.6 마무리 — 12장으로의 다리

이 장에서 한 일을 정리하면 다음과 같다. 제품 entity 트리(6개 층 대응), 기술 entity 트리, *manufactures* 관계(회사→제품), *uses_technology* 관계(제품→기술). 그리고 1편 회상의 결정적 매듭 — NVIDIA B200이 CPO를 핵심 기술로 채택했다는 사실 — 이 데이터에 박혔다.

이제 *회사*, *제품*, *기술*이 모두 데이터로 짜였다. 그러나 — 가장 중요한 한 가지가 아직이다. **회사 사이의 공급 관계.** SK하이닉스가 NVIDIA에 HBM3E를 *공급한다*는 사실, ASML이 TSMC에 EUV 시스템을 *공급한다*는 사실, 한미반도체가 SK하이닉스에 TC Bonder를 *공급한다*는 사실. 이 관계가 들어가야 — *밸류체인*이라는 단어가 데이터 안에서 진짜 의미를 갖는다.

다음 장이 그 자리다. 핵심 공급 관계 10여 개가 데이터로 들어가고, 그 위에서 8장의 재귀 함수가 본격적으로 작동한다. *NVIDIA의 모든 직간접 공급사*가 — 데이터 안에서 처음으로 자동으로 펼쳐진다.
