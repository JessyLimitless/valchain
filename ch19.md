# 제19장. 공급 병목의 정교화 — 부분 의존도가 쌓이는 자리

## 들어가며

17장과 18장에서 *단일 공급원의 매듭*과 *부정 신호의 전파*를 다뤘다. `is_sole_source true`로 표시된 매듭이 — 시스템이 즉시 식별하는 결정적 자리였다.

그러나 — 실제 도메인의 리스크는 *단일 공급원의 매듭에만* 있지 않다. *부분 의존도가 누적된 자리*가 — 보이지 않는 곳에서 더 큰 위험을 만든다. 단일 공급원은 *명시적으로 표시되어 있어서* 시스템도 분석가도 즉시 본다. 그러나 *서너 개 공급사에 30%, 25%, 20%씩 분산된 의존도*는 — 합치면 75%지만, *어느 한 매듭도 단일 공급원으로 표시되지 않는다*. 표시되지 않는 곳에서 — 의존도는 쌓인다.

이 장은 그 *쌓이는 자리*를 시스템적으로 찾아내는 자리다. 17~18장이 *명시된 매듭*을 다뤘다면, 19장은 *합쳐서 보이는 매듭*을 다룬다. 시스템이 더 정교해진다.

---

## 19.1 단일 공급원을 넘어서 — 부분 의존도의 누적

먼저 도메인 사고법부터 짚어둔다. 한 회사가 *공급망에 얼마나 묶여 있는가*는 — 두 가지 방식으로 측정된다.

**방식 1 — 단일 공급원 표시.** 17장에서 다룬 자리. *대체 공급사가 없는* 매듭. 명시적이고, 시스템이 즉시 식별한다.

**방식 2 — 매출 비중의 합.** 한 회사의 매출 중, *특정 공급망 위치에 의존하는 부분*의 합. SK하이닉스가 *NVIDIA에 30% + 메모리 직판 시장에 25% + 다른 빅테크에 20%*로 분산된 매출 구조라고 해도 — *AI 칩 시장 전체*에 묶인 비중을 합치면 *75%가 한 거시 흐름에 묶여 있다*. AI 칩 수요가 무너지면 — 그 한 회사의 매출 3/4이 동시에 흔들린다.

이 두 가지가 — *동시에 분석되어야* 진짜 리스크가 보인다. 단일 공급원만 보면, *합쳐서 위험한 자리*가 놓친다. 매출 비중만 보면, *대체 불가능한 자리*가 흐려진다. 둘이 합쳐져야 — *공급 병목의 진짜 지도*가 그려진다.

19장이 이 두 가지를 시스템에 박는 자리다.

---

## 19.2 의존도 점수 함수 — 한 회사가 한 공급사에 얼마나 묶여 있는가

`revenue_share_in_customer`는 12장에서 박았다. *공급사의 매출 중, 그 고객이 차지하는 비율*. 그러나 18.5에서 짚었듯이 — 이 속성은 *직접 공급 관계*에만 박혀 있다. *간접 공급 관계*의 의존도는 — *직접 공급 라인의 비중을 곱해서 추정*하는 작업이 필요하다.

먼저 직접 공급 의존도를 추출하는 함수.

```typeql
transaction schema optical_chain
```

```typeql
define
  # 한 공급사가 한 고객에 의존하는 매출 비중
  fun direct_dependency($supplier: company, $customer: company) -> { double }:
    match
      $supply (supplier: $supplier, customer: $customer) isa supplies;
      $supply has revenue_share_in_customer $share;
    return { $share };

commit;
```

호출.

```typeql
transaction read optical_chain
```

```typeql
match
  $hynix isa company, has name "SK하이닉스";
  $nvda isa company, has name "NVIDIA";
  let $share in direct_dependency($hynix, $nvda);
fetch
  $share;
```

답:

```json
{ "share": 0.30 }
```

SK하이닉스가 NVIDIA에 30% 의존. 12장 데이터의 직접 답.

여기서 더 흥미로운 자리는 — *한 공급사가 여러 고객에 의존*하는 패턴이다. 동진쎄미켐을 본다.

```typeql
match
  $dongjin isa company, has name "동진쎄미켐";
  
  (supplier: $dongjin, customer: $customer) isa supplies;
  $supply (supplier: $dongjin, customer: $customer) isa supplies;
  $supply has revenue_share_in_customer $share;
  
  $customer has name $cname;
fetch
  $cname; $share;
```

답:

```json
{ "cname": "TSMC", "share": 0.05 }
{ "cname": "삼성전자", "share": 0.25 }
{ "cname": "SK하이닉스", "share": 0.20 }
```

동진쎄미켐의 매출 구조. 세 회사에 *분산된 의존*이다. 어느 한 고객도 *압도적이지는 않다*. 그러나 — *세 의존도의 합은 50%*. 동진쎄미켐 매출의 절반이 *반도체 메모리·파운드리 산업 전체*에 묶여 있다. 한 고객의 비중이 작아도, *섹터 전체*에 대한 의존도는 크다.

이걸 *섹터 의존도*로 합산하는 함수를 짠다.

```typeql
transaction schema optical_chain
```

```typeql
define
  # 한 공급사의 매출 중 반도체 섹터에 묶인 비중의 합
  fun semiconductor_sector_dependency($supplier: company) -> { double }:
    match
      $customer isa company;
      { $customer isa ai_chip_designer; } or
      { $customer isa hbm_maker; } or
      { $customer isa advanced_packaging_house; };
      
      $supply (supplier: $supplier, customer: $customer) isa supplies;
      $supply has revenue_share_in_customer $share;
    return { sum($share) };

commit;
```

함수의 마지막 줄 — `return { sum($share) }`이 결정적이다. *집계 함수*. 여러 의존도를 더해서 한 숫자로. TypeDB 3.0의 Function이 *값 변환과 집계*를 함수 단위에서 직접 처리한다.

호출.

```typeql
match
  $dongjin isa company, has name "동진쎄미켐";
  let $total in semiconductor_sector_dependency($dongjin);
fetch
  $total;
```

답:

```json
{ "total": 0.50 }
```

동진쎄미켐 매출의 50%가 — *반도체 메모리·파운드리 섹터 전체*에 묶여 있다. 단일 고객 의존도로는 *어느 한 매듭도 25% 이하*지만, 섹터 의존도는 *절반*이다.

이게 *부분 의존도의 누적*이 보이는 자리다. 단일 공급원 표시로는 보이지 않는 리스크가, *섹터 단위 합산*에서 드러난다.

---

## 19.3 임계점의 매듭 — 의존도가 위험 수준에 닿는 자리

섹터 의존도가 *얼마나 크면 위험인가*. 정해진 답은 없다. 산업 분석가들마다 다른 임계점을 본다. 30%, 50%, 70%. 보수적인 분석가는 30% 이상을 *집중 리스크*로 본다. 더 관대한 분석가는 70% 이상만 *결정적 의존*으로 본다.

이 책은 — *임계점 자체를 데이터화하는* 모양으로 짠다. 임계점을 함수의 매개변수로 받는 함수.

```typeql
transaction schema optical_chain
```

```typeql
define
  # 섹터 의존도가 임계점 이상인 공급사들
  fun suppliers_above_threshold($threshold: double) -> { company }:
    match
      $supplier isa company;
      let $total in semiconductor_sector_dependency($supplier);
      $total >= $threshold;
    return { $supplier };

commit;
```

함수가 *임계점*을 받는다. 이게 *시스템의 유연성*이다. 분석가가 *오늘은 30%로 보고 싶다*, *내일은 50%로 보고 싶다*를 — *함수 호출 시점*에 결정한다. 함수의 코드는 변하지 않는다.

호출 — 30% 이상.

```typeql
match
  let $supplier in suppliers_above_threshold(0.30);
  $supplier has name $sname;
fetch
  $sname;
```

답:

```json
{ "sname": "한미반도체" }
{ "sname": "ASML" }
{ "sname": "동진쎄미켐" }
```

세 회사. 데이터베이스의 현재 회사 중 *반도체 섹터에 30% 이상 매출이 묶인* 회사들.

한미반도체가 답에 있는 이유 — SK하이닉스에 40% + 삼성에 15%가 *섹터 의존도 55%*. ASML이 답에 있는 이유 — TSMC에 35%가 그대로 *섹터 의존도 35%* (다른 ASML 고객은 데이터에 없음). 동진쎄미켐이 답에 있는 이유 — 19.2에서 본 50%.

임계점을 *50%*로 올리면 — *동진쎄미켐과 한미반도체*만 답에 남는다. 70%로 올리면 — *현재 데이터로는 답이 없다*. 임계점이 분석의 모양을 결정한다.

이 함수가 — *5편 알파 발굴의 정교화*에서 결정적으로 쓰인다. *어느 후속 노드가 진짜 의미 있는 자리인가*를 묻는 자리에서, *그 회사의 섹터 의존도*가 답의 가중치가 된다. 15장의 시차 알파 함수의 출력에 — 19장의 의존도 점수가 결합된 *정교화된 함수*가 5편에서 짜인다.

---

## 19.4 기술 의존도 — 제품이 한 기술에 묶여 있는 모양

공급 의존도가 *회사 사이의 매출 관계*라면, **기술 의존도**는 *제품과 기술 사이의 관계*다. 한 제품이 *몇 개의 기술에 묶여 있고, 그 중 어느 것이 결정적인가*. 11장에서 박은 `uses_technology` 관계와 `is_critical` 속성이 — 이 자리에서 본격적으로 쓰인다.

NVIDIA B200을 본다. 11장에서 *CoWoS-L, HBM3E, CPO* 세 기술이 모두 *is_critical true*로 박혔다.

```typeql
match
  $b200 isa gpu, has model_name "B200";
  $tech isa technology, has technology_name $tname;
  
  $usage (user_product: $b200, used_technology: $tech) isa uses_technology;
  $usage has is_critical $critical;

fetch
  $tname; $critical;
```

답:

```json
{ "tname": "CoWoS-L", "critical": true }
{ "tname": "HBM3E", "critical": true }
{ "tname": "Co-Packaged Optics", "critical": true }
```

세 기술 모두가 *결정적*이다. 어느 하나라도 흔들리면 — B200 전체가 영향을 받는다. 이게 *기술 의존도*의 모양이다.

이걸 함수로 묶는다.

```typeql
transaction schema optical_chain
```

```typeql
define
  # 한 제품의 결정적 기술 의존
  fun critical_technologies($product: product) -> { technology }:
    match
      $usage (user_product: $product, used_technology: $tech) isa uses_technology;
      $usage has is_critical true;
    return { $tech };

commit;
```

호출.

```typeql
match
  $b200 isa gpu, has model_name "B200";
  let $tech in critical_technologies($b200);
  $tech has technology_name $tname;
fetch
  $tname;
```

세 기술이 답으로 나온다. 그러나 — *진짜 정교화*는 그 다음이다. *각 결정적 기술이 어느 공급사에 묶여 있는가*까지 함께 도출.

```typeql
transaction schema optical_chain
```

```typeql
define
  # 한 제품의 결정적 기술 의존 + 각 기술의 공급사
  fun critical_technology_suppliers($product: product) -> { company }:
    match
      let $tech in critical_technologies($product);
      
      # 이 기술을 사용하는 다른 제품
      ($other_product) isa uses_technology;
      ($other_product, $tech) isa uses_technology;
      
      # 그 제품을 만드는 회사
      (maker: $supplier, manufactured_product: $other_product) isa manufactures;
    return { $supplier };

commit;
```

호출.

```typeql
match
  $b200 isa gpu, has model_name "B200";
  let $supplier in critical_technology_suppliers($b200);
  $supplier has name $sname;
fetch
  $sname;
```

답:

```json
{ "sname": "TSMC" }
{ "sname": "SK하이닉스" }
{ "sname": "삼성전자" }
```

세 회사. NVIDIA B200의 *결정적 기술 의존 전체*가 — 이 세 회사로 *수렴*한다. TSMC가 CoWoS-L을, SK하이닉스와 삼성이 HBM3E를. (CPO는 데이터에 *그 기술을 제품으로 만드는 회사*가 박혀 있지 않아서 — 함수 답에 빠졌다. 데이터 확장이 필요한 자리.)

이게 — *기술 의존도가 회사 의존도로 변환되는* 자리다. NVIDIA가 *기술적으로 어느 회사에 묶여 있는가*가 — 한 함수의 답으로 자동 도출된다. *결정적 기술의 핵심 공급사*는, NVIDIA의 *진짜 핵심 리스크 매듭*이다. TSMC나 SK하이닉스에 문제가 생기면 — NVIDIA의 차세대 칩이 *기술적으로 막힌다*.

이 결과가 — 17·18장의 단일 공급원 분석과 합쳐진다. TSMC는 *단일 공급원*이면서 *결정적 기술의 공급사*다. 두 종류의 리스크가 *겹친 자리*. 이런 *겹친 자리*가 — 4편의 핵심 결론이 된다. 다음 장에서 본격적으로 정리한다.

---

## 19.5 정직한 한계 — 의존도 데이터의 실효성

이 장의 함수들은 — *매출 비중 데이터*에 크게 의존한다. 그러나 12장에서 정직하게 짚어두었듯이, *책의 매출 비중 수치는 시연을 위한 추정치*다. 실제 시스템에서는 — *각 회사의 분기·연간 보고서*, *DART 공시*, *분석가 리포트*에서 *실측 매출 비중*을 추출하는 작업이 필요하다.

그리고 — 매출 비중이 *정확*해도, 한 가지 더 어려운 문제가 있다. *매출 비중의 시점성*. 한 회사의 *지금 매출 비중*과 *6개월 뒤 매출 비중*은 다를 수 있다. 큰 거래가 추가되거나 끊어지면 — 의존도 그림이 *순식간에 바뀐다*. 시스템이 *현재 시점의 분석*만 제공한다는 한계를 인지하고, *데이터 갱신 주기*를 명시적으로 관리해야 한다.

또 한 가지 — *수직 계열화의 처리*. 12장의 단순한 *공급사 ↔ 고객* 모델은 — 한 그룹 내의 *수직 계열화 의존도*를 표현하기 어렵다. 가령 삼성전자 메모리 사업부가 삼성전자 시스템LSI에 공급하는 *내부 거래*는, *외부 거래와 다른 종류의 리스크 구조*를 갖는다. 더 정교한 모델에서는 — *intra-group_supply* 같은 별도 관계로 다뤄야 한다.

이런 한계에도 불구하고 — 19장의 함수들은 *기본적 정교화*를 제공한다. 단일 공급원만 보던 시스템이 — *섹터 의존도 합산*과 *기술 의존도 추적*까지 확장됐다. 시스템의 분석 능력이 — 한 단계 깊어졌다.

---

## 19.6 20장으로의 다리

이 장에서 한 일을 정리한다. *direct_dependency* 함수(직접 의존도), *semiconductor_sector_dependency* 함수(섹터 의존도 합산), *suppliers_above_threshold* 함수(임계점 기반 식별), *critical_technologies*와 *critical_technology_suppliers* 함수(기술 의존도 → 공급사 의존도 변환).

17·18장의 *명시적 단일 공급원*에서, 19장의 *부분 의존도 누적*까지. 시스템이 더 정교한 자리에 닿았다.

그러나 — 이 모든 분석은 *전체 도메인*을 본 것이다. *1편 회상의 세 회사* — 광전자, 이노인스트루먼트, 대한광통신 — 가 4편의 부정 신호 분석에서 어떤 자리에 있는지는, 아직 회수되지 않았다. 다음 장이 그 자리다.

3편의 상승 신호 분석에서 *그 세 회사는 같은 사건의 같은 자리에 묶여 있었다*. 4편의 부정 신호에서는 어떨까. 같은 자리일까, 다른 자리일까. 분석가의 머릿속이 *상승의 후보만 외워둔* 비대칭은 — 부정 신호에서도 마찬가지일까. 다음 장이 그 답이다.

4편이 1편 회상과 만나는 자리. 시스템이 *상승*과 *하락*을 *동등한 정밀도*로 다룬다는 약속이, 그 만남에서 확인된다.
