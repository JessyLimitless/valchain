# 제2편. 손에 익히는 TypeDB 3.0

## 편을 시작하며

1편의 마지막에 나는 한 가지를 적어두었다. *회상은 여기까지다. 다음부터는 손을 움직일 차례다.*

이 편은 그 약속을 지키는 자리다. 4개 장에 걸쳐 TypeDB라는 그릇을 *직접 설치하고, 직접 데이터를 넣고, 직접 질문을 던지고, 직접 함수를 짠다*. 코드가 본진이다. 회상의 호흡은 *짧은 한 줄로 가끔* 이어진다 — *그 새벽 종이 위에 그렸던 그림의 어느 조각이 이제 데이터로 옮겨진다*는 자리에서.

손을 움직이는 일에는 한 가지 약속이 있다. **이 편의 모든 코드는 복사해서 그대로 돌아간다.** 책을 손에 들고 키보드를 두드리는 독자가 *책의 예시가 작동하지 않는 자리에서 멈춰버리는* 일은 — 책이 자기 자신과 맺은 약속을 깨는 일이다. 그래서 이 편의 코드는 모두 TypeDB 3.0.0 이상 환경에서 검증됐고, 사용된 문법은 모두 공식 문서를 기준으로 적혔다.

도구를 손에 익힌다는 것은 — *그것이 무엇을 할 수 있는지를 아는 일*이 아니라 *그것을 통해 무엇을 짤 수 있는지를 손이 아는 일*이다. 두 가지는 다르다. 머리로 아는 도구는 *6개월 뒤에 잊혀진다*. 손이 아는 도구만 — 6개월 뒤에도 같은 자리에서 다시 일을 한다.

이 편이 그 *손이 아는 단계*까지 데려가는 것을 약속한다.

---

# 제5장. 환경 구축 — 그릇을 손에 들기

## 들어가며

4장 끝에서 나는 TypeDB라는 이름에 도달했다. 그러나 *이름을 안 것*과 *그 도구를 손에 든 것*은 다른 일이다. 이 장은 그 *손에 드는 자리*다.

본격 코드로 들어가기 전에, 한 가지를 짚어둔다. 이 책에서 사용할 환경은 **TypeDB 3.0 이상**이다. 2024년 12월에 발표된 이 버전은 엔진이 Rust로 다시 짜였고, 이전 2.x 버전과는 *문법이 호환되지 않는다*. 가장 큰 차이는 추론 도구 — 2.x의 *Rule*이 3.0의 *Function*으로 대체됐다. 이 책의 모든 코드는 3.0 문법을 따른다.

만약 인터넷에서 TypeDB 예제를 검색하다가 `rule { ... }` 같은 문법을 보게 된다면 — 그것은 2.x 시대의 코드다. 이 책의 예제와 함께 쓰면 작동하지 않는다. 이 점만 짚어두고 시작한다.

---

## 5.1 TypeDB 3.0 설치

설치 방법은 세 가지다. 각각 자리가 다르다.

**첫째, Docker.** 이 책에서 끝까지 쓸 방법이다. 안정적이고, 환경 차이가 없고, 책의 모든 예제가 그대로 돌아간다. 운영체제와 무관하게 같은 명령으로 실행된다.

```bash
docker pull typedb/typedb:3.0.0
docker run -d -p 1729:1729 --name typedb typedb/typedb:3.0.0
```

두 줄이다. 첫 줄로 이미지를 받고, 둘째 줄로 컨테이너를 띄운다. 포트 1729는 TypeDB의 기본 포트다. 서버가 떴는지 확인하려면 — 다음 명령으로 컨테이너 상태를 본다.

```bash
docker ps | grep typedb
```

`STATUS` 컬럼에 `Up`이 보이면 — 서버가 작동 중이다. 만약 컨테이너가 보이지 않으면, `docker logs typedb` 명령으로 로그를 확인한다.

**둘째, 호스트 설치.** macOS나 Linux에 직접 바이너리를 설치하는 방법이다. Docker가 어려운 환경에서 쓴다. macOS는 Homebrew로 가능하다.

```bash
brew install typedb/tap/typedb
typedb server
```

Linux는 공식 사이트 (typedb.com/download)에서 압축 파일을 받아 압축 해제 후 `typedb server` 명령을 실행한다.

**셋째, TypeDB Cloud.** TypeDB Inc.가 운영하는 호스팅 서비스. 설치 없이 웹 브라우저로 접속해서 바로 쓸 수 있다. 무료 티어가 있어 학습용으로 충분하다. 그러나 — 이 책에서는 *로컬 환경*을 기준으로 짠다. 데이터를 직접 보유하고, 코드를 자유롭게 실험하고, 인터넷 연결과 무관하게 작업할 수 있다는 점에서 로컬이 학습에는 더 적합하다.

이 책의 모든 예제는 첫 번째 방법 — **Docker 기반 TypeDB 3.0** — 을 전제한다. 다른 방법으로 설치한 경우에도 코드는 같지만, 접속 방식과 일부 설정이 다를 수 있다.

---

## 5.2 TypeDB Studio — 시각적 도구

서버가 떴으니, 그 서버에 어떻게 접속하는가. 두 가지 방법이 있다.

**첫째, TypeDB Studio.** 공식 GUI 도구다. 데이터를 시각적으로 확인하고, 쿼리를 작성하고, 결과를 그래프로 본다. 학습 단계에서는 거의 필수에 가깝다. 공식 사이트에서 운영체제에 맞는 버전을 받아 설치한다 (typedb.com/download).

설치 후 실행하면, 첫 화면에서 *서버 연결*을 요구한다. 로컬에서 띄운 서버라면 다음 정보를 입력한다.

- 주소: `localhost:1729`
- 사용자명: `admin`
- 비밀번호: `password` (기본값)

기본 비밀번호는 첫 로그인 시 변경하라는 경고가 뜬다. 학습용 로컬 환경에서는 그대로 둬도 무방하지만, 외부에 노출되는 서버라면 반드시 변경한다.

**둘째, TypeDB Console.** 명령줄 도구. Docker 컨테이너 안에 함께 들어 있다. 가벼운 작업이나 자동화 스크립트에 쓴다.

```bash
docker exec -it typedb typedb console
```

이 명령으로 console에 접속하면, `>>` 프롬프트가 뜬다. 거기서 직접 TypeQL을 입력한다. 책의 예제는 대부분 — Studio에서 작업하는 것을 가정한다. Studio가 *문법 강조, 자동 완성, 결과 시각화*를 모두 제공하기 때문이다.

---

## 5.3 첫 데이터베이스 — `optical_chain`

서버가 떴고, Studio가 연결됐다. 이제 *책 전체에서 쓸 데이터베이스*를 만든다.

Studio의 왼쪽 패널에 *Databases* 항목이 보인다. 거기서 *Create database* 버튼을 누르고, 이름을 입력한다.

```
optical_chain
```

광통신 밸류체인을 의미하는 이름이다. 1편에서 회상한 그 도메인 — 광전자, 이노인스트루먼트, 대한광통신을 비롯한 NVIDIA 광 인터커넥트 신호 경로 위의 회사들 — 이 이 데이터베이스 안에 짜인다. 9장 이후에서 본격적으로 채워진다.

이 책의 모든 예제는 이 데이터베이스 안에서 작동한다. 다른 이름을 쓰고 싶다면 — 코드의 데이터베이스 이름만 바꾸면 된다.

Console에서도 같은 작업을 할 수 있다.

```typeql
database create optical_chain
```

생성 후, *어느 데이터베이스에서 작업할지*를 선택해야 한다. Studio에서는 좌측 패널에서 데이터베이스 이름을 클릭하면 된다. Console에서는 다음 명령으로 *그 데이터베이스를 사용한다는 선언*을 한다.

```typeql
transaction schema optical_chain
```

이 명령은 *스키마 작업 모드*로 진입한다. TypeDB는 트랜잭션 모드가 두 가지다 — `schema`(스키마 정의·변경)와 `write`(데이터 입력·수정)와 `read`(데이터 조회). 어느 모드에 들어와 있는지가 *어떤 작업을 할 수 있는지*를 결정한다.

---

## 5.4 손이 움직이는 자리 — 첫 번째 define

이제 손이 움직인다. 처음으로 *그 새벽 종이 위 그림*의 한 조각을 데이터로 옮긴다.

가장 작은 조각부터 시작한다. *회사*라는 개체 하나, 그리고 광전자라는 회사의 이름.

스키마 모드에서, 다음을 입력한다.

```typeql
define
  entity company,
    owns name,
    owns ticker;
  
  attribute name, value string;
  attribute ticker, value string;
```

여섯 줄이다. 의미는 다음과 같다.

- `entity company` — *회사*라는 개체 종류가 존재한다.
- `owns name` — 회사는 *이름*이라는 속성을 가진다.
- `owns ticker` — 회사는 *티커*(종목 코드)라는 속성을 가진다.
- `attribute name, value string` — *이름*은 문자열 값이다.
- `attribute ticker, value string` — *티커*도 문자열 값이다.

여기까지가 *스키마*다. 회사라는 종류가 *존재할 수 있다*는 약속만 적었다. 아직 어떤 회사도 실제로 들어 있지는 않다.

스키마를 저장하려면 — 트랜잭션을 *커밋*해야 한다. Studio에서는 우측 상단의 *Commit* 버튼을 누른다. Console에서는 `commit` 명령을 입력한다.

```typeql
commit
```

커밋이 성공하면, 스키마가 데이터베이스에 박힌다.

이제 데이터를 넣어 본다. 이번에는 *쓰기 모드*로 진입한다.

```typeql
transaction write optical_chain
```

그리고 광전자라는 회사 하나를 입력한다.

```typeql
insert
  $kwangjeon isa company,
    has name "광전자",
    has ticker "017900";
```

세 줄이다. `$kwangjeon`은 *변수* — 이 회사를 일시적으로 가리키는 이름이다. `isa company`는 *광전자는 company 종류의 개체다*라는 선언. `has name "광전자"`는 *이 회사의 이름이 "광전자"다*라는 사실. `has ticker "017900"`은 광전자의 코스피 종목 코드다.

커밋한다.

```typeql
commit
```

이제 광전자가 *데이터베이스 안*에 있다. 머릿속 그림이 아니라, 적혀 있는 그림 위에. 어쩌다 외워둔 회사가 아니라, 시스템이 *기억하는* 회사로.

확인해 본다. 이번에는 *읽기 모드*다.

```typeql
transaction read optical_chain
```

그리고 질문을 던진다.

```typeql
match
  $c isa company,
    has name $n,
    has ticker $t;
fetch
  $n; $t;
```

답이 돌아온다.

```json
{ "n": "광전자", "t": "017900" }
```

한 회사가 적혔다. 1편 회상의 가장 첫 줄 — *광전자라는 회사명이 내 머릿속의 어디인가에 어쩌다 자리잡고 있었다* — 의 *어쩌다* 부분이 — 이제 *데이터의 명시적 약속*으로 바뀌었다. 누구의 머릿속도 아닌, 데이터베이스 안의 자리에.

그 새벽 8시 58분 HTS 화면 앞 — 매수 버튼 위에서 손가락이 멈춰 있던 자리 — 가 한 부사의 교체로 정정된다. 머릿속을 통과했던 *이거다*가, 데이터베이스 안에서는 *이게 적혀 있다*가 된다. *어쩌다*가 *명시적으로*로 옮겨졌다. 한 부사의 교체 안에, 책 전체의 약속이 들어 있다.

작은 한 걸음이다. 그러나 — 책의 본진은 여기서부터 시작된다.

---

## 5.5 마무리 — 다음 장에서

이 장에서 한 일을 정리하면 다음과 같다. Docker로 TypeDB 3.0 서버를 띄웠고, Studio로 접속했고, `optical_chain`이라는 데이터베이스를 만들었고, 회사라는 entity와 이름·티커라는 두 속성을 정의했고, 광전자라는 첫 회사를 넣었다.

손이 그릇에 닿았다. 그러나 — 한 회사만으로는 그림이 그려지지 않는다. *분류*가 다층이고, *관계*가 N항이고, *자리*가 명시적이고, *시점*이 박혀 있는 그림. 그 그림으로 가려면 — TypeQL의 문법 골격을 더 익혀야 한다.

다음 장은 그 자리다. `define`, `insert`, `match`, `fetch`, `delete`. 다섯 개의 키워드가 TypeQL의 골격을 이룬다. 그 다섯 개를 차례로 본다.
