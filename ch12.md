# 제12장. 공급 관계의 본격 데이터 모델

## 들어가며

11장 끝에 적어두었듯이 — *밸류체인*이라는 단어가 데이터 안에서 진짜 의미를 가지려면, *회사 사이의 공급 관계*가 들어가야 한다. SK하이닉스가 NVIDIA에 HBM3E를 공급하고, ASML이 TSMC에 EUV 시스템을 공급하고, 한미반도체가 SK하이닉스에 TC Bonder를 공급한다는 사실들이 데이터로 박혀야 — 9장의 6개 층 그림이 *층 안의 회사 묶음*에서 *층 사이를 잇는 신호의 그물망*으로 변환된다.

이 장이 그 변환을 짜는 자리다. 공급 관계를 정의하고, 핵심 10여 개의 관계를 입력하고, 그 위에서 *첫 매듭*을 본다. NVIDIA의 1차 공급사, 2차 공급사, 그리고 — 8장 재귀 함수의 본격 활용으로 — *모든 직간접 공급사*. 데이터의 그림이 처음으로 *진짜 밸류체인*의 모양을 드러낸다.

---

## 12.1 `supplies` 관계의 본격 정의

6장에서 단순한 `supplies` 관계를 한 번 본 적이 있다. 그러나 — *공급 관계의 진짜 모양*은 그것보다 복잡하다. *언제 시작됐는지*, *매출 비중이 얼마인지*, *단일 공급원인지 후속 공급사가 있는지* — 이 모든 정보가 *한 사건 안에* 함께 있어야 한다.

7장의 사고법대로, 모든 도메인 규칙이 *데이터 모델 안*에 박힌다.

```typeql
transaction schema optical_chain
```

```typeql
define
  relation supplies,
    relates supplier @card(1..1),
    relates customer @card(1..1),
    relates supplied_product @card(1..1),
    owns started_year @card(0..1),
    owns revenue_share_in_customer @card(0..1),
    owns is_sole_source @card(0..1);
  
  company plays supplies:supplier;
  company plays supplies:customer;
  product plays supplies:supplied_product;
  
  attribute revenue_share_in_customer, value double @range(0.0..1.0);
  attribute is_sole_source, value boolean;

commit;
```

여기서 결정적 자리는 — `revenue_share_in_customer`와 `is_sole_source`라는 두 속성이다.

**`revenue_share_in_customer`**는 *공급자의 매출 중, 이 고객이 차지하는 비율*이다. 0과 1 사이의 값. SK하이닉스 매출에서 NVIDIA가 30%를 차지한다면 — 이 값은 0.30. 이 정보가 — *공급자가 고객에 얼마나 의존하는지*를 직접 표현한다. 단일 고객의 비중이 너무 크면 — 그 고객이 다른 공급사로 전환할 때의 *충격*이 결정적 리스크가 된다.

**`is_sole_source`**는 *이 제품을 공급하는 유일한 공급자인가*. ASML의 EUV 노광 장비가 가장 대표적인 *sole source* 자리다. 전 세계에서 *한 회사가 만든다*. 이 표시가 있는 매듭은 — 밸류체인 전체의 *결정적 자리*다. 그 한 회사에 문제가 생기면, 위로 산업 전체가 영향을 받는다.

7장에서 적어두었던 *@range*와 함께 — *@card(0..1)*도 의미가 있다. *공급 관계의 일부 속성은 옵션이다*. 모든 공급 관계가 *매출 비중을 안다*고 가정하지 않는다. 비상장 회사 간의 공급 관계는 매출 비중이 공개되지 않는 경우가 많다. 그런 자리에서는 *속성을 비워두고* 관계만 입력한다.

---

## 12.2 핵심 공급 관계 입력

이제 *진짜 공급 관계*들이 들어간다. 책의 후반 분석에서 *결정적으로 쓰이는* 핵심 매듭 12개.

쓰기 모드로 전환:

```typeql
transaction write optical_chain
```

```typeql
match
  # 회사 변수
  $nvda isa company, has name "NVIDIA";
  $tsmc isa company, has name "TSMC";
  $hynix isa company, has name "SK하이닉스";
  $samsung isa company, has name "삼성전자";
  $micron isa company, has name "Micron";
  $hanmi isa company, has name "한미반도체";
  $hpsp isa company, has name "HPSP";
  $asml isa company, has name "ASML";
  $dongjin isa company, has name "동진쎄미켐";
  
  # 제품 변수
  $hbm3e isa hbm, has model_name "HBM3E";
  $cowos_svc isa packaging_service, has model_name "CoWoS-L Service";
  $tc_bonder isa tc_bonder, has model_name "TC Bonder";
  $euv_sys isa euv_lithography_system, has model_name "NXE:3800E";
  $photoresist_p isa photoresist, has model_name "EUV Photoresist";

insert
  # === NVIDIA 1차 공급사 ===
  
  # SK하이닉스 → NVIDIA: HBM3E. 2024년부터. SK 매출 중 NVIDIA 비중 매우 큼.
  (supplier: $hynix, customer: $nvda, supplied_product: $hbm3e) isa supplies,
    has started_year 2024,
    has revenue_share_in_customer 0.30,
    has is_sole_source false;
  
  # 삼성전자 → NVIDIA: HBM3E. 진입은 했으나 후속 공급. 
  (supplier: $samsung, customer: $nvda, supplied_product: $hbm3e) isa supplies,
    has started_year 2024,
    has revenue_share_in_customer 0.03,
    has is_sole_source false;
  
  # Micron → NVIDIA: HBM3E. 가장 후속.
  (supplier: $micron, customer: $nvda, supplied_product: $hbm3e) isa supplies,
    has started_year 2024,
    has revenue_share_in_customer 0.10,
    has is_sole_source false;
  
  # TSMC → NVIDIA: CoWoS-L 패키징. 사실상 단일 공급사 — 최첨단 노드 패키징.
  (supplier: $tsmc, customer: $nvda, supplied_product: $cowos_svc) isa supplies,
    has started_year 2022,
    has revenue_share_in_customer 0.20,
    has is_sole_source true;
  
  # === HBM 메이커의 1차 공급사 ===
  
  # 한미반도체 → SK하이닉스: TC Bonder. SK가 한미의 최대 고객.
  (supplier: $hanmi, customer: $hynix, supplied_product: $tc_bonder) isa supplies,
    has started_year 2018,
    has revenue_share_in_customer 0.40,
    has is_sole_source false;
  
  # 한미반도체 → 삼성전자: TC Bonder (후속).
  (supplier: $hanmi, customer: $samsung, supplied_product: $tc_bonder) isa supplies,
    has started_year 2023,
    has revenue_share_in_customer 0.15,
    has is_sole_source false;
  
  # === TSMC의 1차 공급사 ===
  
  # ASML → TSMC: EUV. 전 세계 단일 공급사.
  (supplier: $asml, customer: $tsmc, supplied_product: $euv_sys) isa supplies,
    has started_year 2019,
    has revenue_share_in_customer 0.35,
    has is_sole_source true;
  
  # === 소재 공급 ===
  
  # 동진쎄미켐 → TSMC: 포토레지스트.
  (supplier: $dongjin, customer: $tsmc, supplied_product: $photoresist_p) isa supplies,
    has started_year 2018,
    has revenue_share_in_customer 0.05,
    has is_sole_source false;
  
  # 동진쎄미켐 → 삼성전자: 포토레지스트.
  (supplier: $dongjin, customer: $samsung, supplied_product: $photoresist_p) isa supplies,
    has started_year 2010,
    has revenue_share_in_customer 0.25,
    has is_sole_source false;
  
  # 동진쎄미켐 → SK하이닉스: 포토레지스트.
  (supplier: $dongjin, customer: $hynix, supplied_product: $photoresist_p) isa supplies,
    has started_year 2015,
    has revenue_share_in_customer 0.20,
    has is_sole_source false;

commit;
```

10개의 공급 관계가 데이터로 박혔다. 이게 — 매우 단순화된 모형이지만 — 현 시점 글로벌 반도체 밸류체인의 *핵심 매듭들*이다.

여기서 짚어둘 정직한 사실 하나가 있다. **위의 매출 비중 수치는 *책의 예시를 위한 추정치*이지, 검증된 공식 수치가 아니다.** 회사들의 분기 보고서, 산업 분석 리포트, 매체 보도에서 추출한 *대략적인 추정*이다. 책의 분석 자체는 이 수치들에 *의존하지 않는다* — 분석은 *구조*에서 나오지, *정확한 매출 비중*에서 나오지 않는다. 다만 *시연 데이터*로서 — 매출 비중이 데이터 모델에 어떻게 들어가는지를 보이기 위한 자리다.

실제 분석에 쓰려면, 각 회사의 *공식 IR 자료, DART 공시, 분기 컨퍼런스 콜 내용*을 데이터의 출처로 박는 별도의 *증거 관계*가 필요하다. 14장에서 시점 정보가 들어갈 때 — 그 *증거의 자리*도 함께 짚는다.

---

## 12.3 첫 매듭 — NVIDIA의 1차 공급사

데이터가 들어갔으니, 첫 분석을 던진다. 가장 단순한 질문 — *NVIDIA에 직접 공급하는 모든 회사*.

```typeql
transaction read optical_chain
```

```typeql
match
  $nvda isa company, has name "NVIDIA";
  $supplier isa company, has name $sname;
  $product isa product, has model_name $pname;
  $supply (supplier: $supplier, customer: $nvda, supplied_product: $product) isa supplies;
  $supply has started_year $year;

fetch
  $sname; $pname; $year;
```

답:

```json
{ "sname": "SK하이닉스", "pname": "HBM3E", "year": 2024 }
{ "sname": "삼성전자", "pname": "HBM3E", "year": 2024 }
{ "sname": "Micron", "pname": "HBM3E", "year": 2024 }
{ "sname": "TSMC", "pname": "CoWoS-L Service", "year": 2022 }
```

네 행이 답으로 나왔다. NVIDIA의 1차 공급사 네 곳이 — 각자 무엇을, 언제부터 공급하기 시작했는지와 함께 — 데이터 안에서 *자동으로* 드러난다.

이 답이 *단순해 보여도* — 1편 회상의 시각으로 보면 다른 의미를 가진다. 4월 1일 새벽, 이 답이 *그 시점에 손에 있었더라면*. NVIDIA의 직접 공급사 네 곳을 *외워둘 필요가 없었다*. 시스템이 답해줬다. 그리고 — 이 답에 *광전자, 이노인스트루먼트, 대한광통신*은 *없다*. 그들은 *직접* 공급사가 아니기 때문이다. *그러나 신호 경로 위에는 있다*는 사실은, 다음 절들에서 본격적으로 드러난다.

### 단일 공급원 필터

매듭 분석의 한 변형. 위 네 공급 관계 중 *단일 공급원*인 자리만 본다.

```typeql
match
  $nvda isa company, has name "NVIDIA";
  $supplier isa company, has name $sname;
  $product isa product, has model_name $pname;
  $supply (supplier: $supplier, customer: $nvda, supplied_product: $product) isa supplies;
  $supply has is_sole_source true;

fetch
  $sname; $pname;
```

답:

```json
{ "sname": "TSMC", "pname": "CoWoS-L Service" }
```

TSMC 하나. *NVIDIA가 최첨단 패키징에서 단일 공급원에 의존하고 있다*는 사실이 — 데이터 모델 안에서 한 줄의 쿼리로 드러난다. 이건 *밸류체인의 리스크 매듭*이다. TSMC에 문제가 생기면, NVIDIA의 최첨단 칩 공급 자체가 흔들린다.

같은 방식으로 *TSMC의 단일 공급원*은 ASML이고, 이 한 회사 — ASML — 가 *글로벌 반도체 산업 전체의 단일 매듭*이 되는 자리다. 4편 *리스크 분석*에서 본격적으로 다루는 자리다.

---

## 12.4 두 단계 매듭 — `direct_suppliers_of` 함수

매번 *직접 공급사*를 묻는 query를 짜는 것은 번거롭다. 8장에서 본 *Function*으로 묶는다.

```typeql
transaction schema optical_chain
```

```typeql
define
  fun direct_suppliers_of($customer: company) -> { company }:
    match
      ($supplier, $customer, $product) isa supplies;
    return { $supplier };

commit;
```

함수 하나가 정의됐다. 8장의 동일한 패턴이지만, 이제 *진짜 도메인 데이터* 위에서 작동한다.

호출해 본다.

```typeql
transaction read optical_chain
```

```typeql
match
  $nvda isa company, has name "NVIDIA";
  let $supplier in direct_suppliers_of($nvda);
  $supplier has name $sname;

fetch
  $sname;
```

답:

```json
{ "sname": "SK하이닉스" }
{ "sname": "삼성전자" }
{ "sname": "Micron" }
{ "sname": "TSMC" }
```

4개의 직접 공급사. 12.3과 같은 답이지만, *함수 호출*로 묶였다. 그러면 — *2차 공급사*는 어떻게 찾을까?

SK하이닉스의 직접 공급사가 *NVIDIA의 2차 공급사*다. 한미반도체와 동진쎄미켐. 이걸 함수 합성으로 풀 수 있다.

```typeql
match
  $nvda isa company, has name "NVIDIA";
  let $first_tier in direct_suppliers_of($nvda);
  let $second_tier in direct_suppliers_of($first_tier);
  $first_tier has name $first_name;
  $second_tier has name $second_name;

fetch
  $first_name; $second_name;
```

답:

```json
{ "first_name": "SK하이닉스", "second_name": "한미반도체" }
{ "first_name": "SK하이닉스", "second_name": "동진쎄미켐" }
{ "first_name": "삼성전자", "second_name": "한미반도체" }
{ "first_name": "삼성전자", "second_name": "동진쎄미켐" }
{ "first_name": "TSMC", "second_name": "ASML" }
{ "first_name": "TSMC", "second_name": "동진쎄미켐" }
```

여섯 행. NVIDIA의 *두 단계 떨어진 공급사들*이 — 각자 어느 1차 공급사를 통해 NVIDIA에 닿는지와 함께 — 자동으로 펼쳐졌다. 한미반도체와 동진쎄미켐과 ASML이 — 분석에 처음으로 *NVIDIA 신호 경로 위에 있는 회사*로 등장한다.

특히 — 같은 *동진쎄미켐*이 세 번 등장한다. SK하이닉스를 통해, 삼성전자를 통해, TSMC를 통해. 한 회사가 *여러 경로로* NVIDIA에 닿는다는 사실이 — 데이터 안에서 보인다. 9장의 *매듭은 점이 아니다*라는 비유의 첫 실제 시연이다.

---

## 12.5 `all_suppliers_of` — 모든 직간접 공급사

여기까지는 *두 단계*까지였다. *모든 직간접 공급사*는 — 8장의 재귀 함수로 풀린다.

```typeql
transaction schema optical_chain
```

```typeql
define
  fun all_suppliers_of($customer: company) -> { company }:
    # 기저 — 직접 공급사
    match
      ($direct, $customer, $product) isa supplies;
    return { $direct };
    
    or
    
    # 재귀 — 중간 공급사의 공급사
    match
      ($mid, $customer, $product) isa supplies;
      let $indirect in all_suppliers_of($mid);
    return { $indirect };

commit;
```

8장과 정확히 동일한 패턴. 호출.

```typeql
transaction read optical_chain
```

```typeql
match
  $nvda isa company, has name "NVIDIA";
  let $any_supplier in all_suppliers_of($nvda);
  $any_supplier has name $sname;

fetch
  $sname;
```

답 (중복 제거됨):

```json
{ "sname": "SK하이닉스" }
{ "sname": "삼성전자" }
{ "sname": "Micron" }
{ "sname": "TSMC" }
{ "sname": "한미반도체" }
{ "sname": "ASML" }
{ "sname": "동진쎄미켐" }
```

7개 회사. **NVIDIA의 모든 직간접 공급사**가 — 직접 공급사 4개 + 간접 공급사 3개 — 데이터 안에서 자동으로 드러난다. 이 7개 회사가 — *NVIDIA의 한 결정이 즉시 영향을 미치는 회사들*이다.

여기서 다시 한 번 짚어둔다. *이 답에 광전자, 이노인스트루먼트, 대한광통신은 여전히 없다*. 그들은 *공급망에는 없다*. 그러나 — 신호는 도달한다. 그 *신호의 경로*는 *공급 관계*가 아니라 *기술 채택*을 통해 흐른다. 다음 장(13장)에서 그 *기술 채택 관계*가 들어가면, 그제야 — 1편 회상의 세 회사가 *데이터 안의 신호 경로 위*에 자리를 잡는다.

---

## 12.6 마무리 — 13장으로의 다리

이 장에서 한 일을 정리한다. *supplies* 관계의 본격 정의(시점, 매출 비중, 단일 공급원 여부 포함), 10개의 핵심 공급 관계 입력, *direct_suppliers_of*와 *all_suppliers_of* 함수의 본격 활용. NVIDIA를 중심으로 한 *7개 직간접 공급사*가 데이터 안에서 자동으로 펼쳐졌다.

그러나 — 12.5 마지막에 적었듯이 — *광전자, 이노인스트루먼트, 대한광통신은 이 답에 없다*. 그들은 공급망 안에 있지 않다. 신호는 *공급의 흐름*이 아니라 *기술 채택의 흐름*을 통해 그들에게 도달한다.

다음 장이 그 흐름의 자리다. **기술 채택 발언** — NVIDIA의 한 결정이 *공급망 밖의 회사들*에까지 신호를 보내는 모양. 1편 회상의 *NVIDIA 광반도체 발언*이 데이터로 박히고, 그 발언의 *영향 경로*가 자동으로 도출된다. 광전자, 이노인스트루먼트, 대한광통신이 — 그제야 *데이터 안의 신호 경로 위*에 자리를 찾는다.
