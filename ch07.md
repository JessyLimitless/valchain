# 제7장. 스키마 사고법 — 좋은 그릇과 나쁜 그릇

## 들어가며

5장에서 그릇을 손에 들었고, 6장에서 다섯 개의 키워드로 작은 모델을 짰다. 문법은 익혔다. 그러나 — 문법을 안다고 *좋은 스키마*가 짜이지는 않는다.

이 장은 *사고법*의 자리다. 같은 도메인을 두 사람이 짜면 — 두 개의 다른 스키마가 나온다. 한 모양은 6개월 뒤에도 *그 자리에서 일을 한다*. 다른 모양은 *3개월 만에 무너진다*. 차이는 문법이 아니라 — *어떤 사고가 그 스키마 뒤에 있었는가*에 있다.

이 장에서 보는 네 가지 사고법이 있다. *분류 가지를 어디까지 쪼갤 것인가*, *같은 회사가 여러 자리에 들어가는 모양을 어떻게 표현할 것인가*, *잘못된 데이터를 어떻게 거부할 것인가*, 그리고 — *좋은 스키마와 나쁜 스키마는 무엇이 다른가*.

이 장의 모든 코드는 `optical_chain` 데이터베이스에서 실행된다. 6장의 미니어처 위에 — 새 정의가 *추가*된다. 기존 데이터를 지울 필요는 없다.

---

## 7.1 분류 가지를 어디까지 쪼갤 것인가

6장에서 회사를 네 가지 하위 종류로 쪼갰다. *AI 칩 설계사*, *HBM 메이커*, *패키징 회사*, *장비 회사*. 그러나 — 이 네 가지가 충분한가?

1편에서 회상한 그 사건을 다시 떠올려 본다. 광전자, 이노인스트루먼트, 대한광통신은 — 이 네 가지 중 어느 것에도 속하지 않는다. 광전자는 *광반도체·광센서* 회사고, 이노인스트루먼트는 *광통신 시공 장비* 회사고, 대한광통신은 *광섬유·광케이블* 회사다. 6장의 네 가지로는 — 1편의 세 회사가 데이터에 들어갈 자리가 없다.

분류를 더 쪼개야 한다.

```typeql
define
  # 6장에서 정의한 네 가지 + 추가 분류
  entity optical_component_maker sub company;
  
  # 광반도체·광통신 부품 회사의 하위 세 가지
  entity optical_semiconductor_maker sub optical_component_maker;
  entity optical_fiber_maker sub optical_component_maker;
  entity optical_installation_equipment_maker sub optical_component_maker;
```

세 단계의 가지가 만들어졌다.

```
company
   │
   ├─ ai_chip_designer
   ├─ hbm_maker
   ├─ packaging_house
   ├─ equipment_maker
   └─ optical_component_maker
        │
        ├─ optical_semiconductor_maker   ← 광전자
        ├─ optical_fiber_maker            ← 대한광통신
        └─ optical_installation_equipment_maker  ← 이노인스트루먼트
```

이제 1편의 세 회사를 데이터에 넣을 자리가 있다. 광전자는 `optical_semiconductor_maker`로, 대한광통신은 `optical_fiber_maker`로, 이노인스트루먼트는 `optical_installation_equipment_maker`로.

그러나 — 여기서 한 가지 질문이 따라온다. **얼마나 더 쪼개야 할 것인가?**

`optical_semiconductor_maker` 안에 *광반도체*와 *광센서*를 또 나눌 수도 있다. 광반도체 안에서 *발광 소자*와 *수광 소자*를 또 나눌 수도 있다. 끝없이 쪼갤 수 있다.

이 자리에서 *분류 깊이*에 대한 첫 번째 사고법이 등장한다.

> **분류는 *질문에 답할 수 있을 만큼만* 쪼갠다.**

만약 *어떤 회사가 광반도체에 영향을 받는가*라는 질문에 답하고 싶다면 — `optical_semiconductor_maker` 한 가지면 충분하다. 더 쪼개면 *답의 단위*가 너무 작아진다. 만약 *발광 소자 공급사와 수광 소자 공급사를 구분해서 분석하고 싶다면* — 더 쪼개야 한다.

분류 깊이는 *도구의 욕심*이 아니라 *질문의 모양*이 결정한다. 짜는 사람이 먼저 *어떤 질문에 답할 것인가*를 정하지 않으면 — 스키마는 *끝없이 자라나거나 의미 없이 단순해진다*.

이 책의 무대인 *NVIDIA 향 반도체 밸류체인 분석*에서는 — 위 세 단계 정도가 자연스러운 깊이다. *분류 가지가 너무 거칠어서 빠뜨리는 회사가 생기는 일*도 막고, *분류가 너무 정밀해서 같은 본질의 회사가 서로 다른 가지로 흩어지는 일*도 막는다. 1편의 *광전자/이노인스트루먼트/대한광통신*이 같은 *optical_component_maker* 가지 안에 있되, 서로 다른 하위로 갈라지는 모양이 — 정확히 그 균형점이다.

---

## 7.2 같은 회사가 여러 자리에 — `plays`의 진짜 가치

6장에서 `plays`라는 키워드를 봤다. *company plays supplies:supplier* 같은 모양. *회사는 공급 관계의 공급자 자리에 들어갈 수 있다*는 약속이었다.

그러나 6장에서는 `plays`의 *진짜 가치*가 드러나지 않았다. 한 회사가 *한 관계의 한 자리*에만 있는 단순한 예제였기 때문이다. 이 장에서는 — 같은 회사가 *여러 관계의 여러 자리*에 있는 모양을 본다.

2편의 핵심 인물인 SK하이닉스를 다시 본다. SK하이닉스는 — 1편 회상에서 적었듯이 — 네 가지 자리에 있다.

- NVIDIA에 HBM을 공급하는 *공급자*
- 한미반도체로부터 본더를 사들이는 *수요자*
- 삼성전자와 HBM 시장에서 다투는 *경쟁자*
- 광반도체 발언 사건의 직접 경로 위에 없는 — *비수혜 회사*

이 네 자리가 *모두 같은 SK하이닉스라는 한 회사에 묶여 있다*는 사실을 — 스키마가 어떻게 표현하는가.

먼저 *경쟁 관계*를 새로 정의한다.

```typeql
define
  # 경쟁 관계 — 두 회사가 한 시장 세그먼트에서 경쟁
  relation competes_in,
    relates competitor,
    relates segment;
  
  # 시장 세그먼트
  entity market_segment,
    owns segment_name;
  
  # 자리 정의
  company plays competes_in:competitor;
  market_segment plays competes_in:segment;
  
  attribute segment_name, value string;

commit;
```

스키마가 추가됐다. 이제 SK하이닉스가 *공급 관계*와 *경쟁 관계*의 양쪽 자리에 들어간다.

데이터를 넣는다.

```typeql
insert
  # 삼성전자 추가
  $samsung isa hbm_maker,
    has name "삼성전자",
    has ticker "005930";
  
  # HBM 시장 세그먼트
  $hbm_market isa market_segment,
    has segment_name "HBM";
  
  # 경쟁 관계 — SK하이닉스가 한 자리, 삼성전자가 다른 자리
  $compete (competitor: $hynix, competitor: $samsung, segment: $hbm_market)
    isa competes_in;

commit;
```

여기서 한 가지 새 문법이 등장한다. `(competitor: $hynix, competitor: $samsung, ...)` — *같은 자리에 두 개체가 함께 들어간다*. 경쟁 관계는 *두 경쟁자가 같은 자리(competitor)에 함께 있는* 모양이다. 공급 관계의 *공급자와 수요자가 다른 자리*에 있는 것과는 다르다.

이게 N항 관계의 또 한 가지 자유로움이다. 한 자리에 *여러 개체*가 들어갈 수 있다. *세 회사가 함께 경쟁하는 시장*도 같은 모양으로 표현된다 — `(competitor: $hynix, competitor: $samsung, competitor: $micron, segment: $hbm_market)`.

이제 SK하이닉스의 *모든 자리*를 한 번에 조회해 본다.

```typeql
transaction read optical_chain
```

```typeql
match
  $hynix isa company, has name "SK하이닉스";
  
  # 공급자 자리
  ($hynix, $customer1, $prod1) isa supplies;
  $customer1 has name $cname;
  $prod1 has model_name $pname;
fetch
  $cname; $pname;
```

답:

```json
{ "cname": "NVIDIA", "pname": "HBM3E" }
```

SK하이닉스가 *공급자* 자리에 있는 한 사건이 — NVIDIA에 HBM3E를 공급하는 것 — 이 답으로 나왔다. 같은 SK하이닉스의 *수요자* 자리는, 한미반도체와의 관계에서 별도 쿼리로 조회할 수 있다. *경쟁자* 자리는 또 다른 쿼리.

여기서 중요한 사실 하나가 또렷해진다. **자리는 관계에 묶여 있다.** SK하이닉스라는 *한 회사*가 — 여러 관계에서 여러 자리에 있을 수 있고, 각 자리는 *그 관계의 의미 안에서만* 작동한다. *공급자*는 supplies 관계 안에서만, *경쟁자*는 competes_in 관계 안에서만 의미를 갖는다.

이게 2장 회상의 세 번째 깨달음 — *역할은 관계에 묶여 있지 회사에 묶여 있지 않다* — 가 스키마 안에서 살아 있는 모양이다.

같은 깨달음의 또 다른 면이 광전자와 이노인스트루먼트의 자리에서 보인다. 2장 카페에서 적었던 그 발견 — *분류 가지가 달라도 같은 사건의 같은 자리에 있을 수 있다* — 가 plays라는 한 단어 위에서 데이터로 변환된다. 광전자는 *광반도체·광센서* 가지에, 이노인스트루먼트는 *광통신 시공 장비* 가지에. 분류 트리에서는 다른 자리지만 — *NVIDIA 광반도체 발언*이라는 사건의 *영향 받는* role에서는 같은 자리에 묶인다. 분류만으로는 묶이지 않는 두 회사가, role의 자리에서는 묶이는 모양. 머릿속 그림이 어쩌다 한 회사만 외워두는 비대칭이 — 이 자리에서야 *원리적으로* 풀린다.

---

## 7.3 새 무기 — `@card`, `@range`, `@values`

지금까지의 스키마는 *어떤 데이터가 있을 수 있는지*를 약속했다. 그러나 *얼마나 있을 수 있는지*, *어떤 값 범위 안에 있어야 하는지*, *허용된 값들 중 어느 것이어야 하는지* — 같은 *세 가지 추가 제약*이 빠져 있다.

TypeDB 3.0은 이런 제약을 위한 키워드를 가지고 있다.

### `@card` — 개수 제약

한 개체가 *어느 속성을 몇 개 가질 수 있는지*를 강제한다.

```typeql
define
  entity company,
    owns name @card(1..1),
    owns ticker @card(0..1);
```

`@card(1..1)`은 *정확히 한 개*를 의미한다. *모든 회사는 이름을 반드시 하나 가져야 하고, 더 가질 수 없다*. `@card(0..1)`은 *없거나 하나*. 비상장 회사는 ticker가 없을 수도 있다는 약속이다.

`@card(1..)`은 *하나 이상*. 한 회사가 *여러 사업 영역*에 속할 수 있다는 약속이라면, 사업 영역 속성을 `@card(1..)`로 적는다.

이게 강제되면 — 잘못된 데이터를 데이터베이스가 *INSERT를 거부*한다. *이름이 두 개인 회사*를 넣으려고 하면, 트랜잭션 자체가 실패한다.

### `@range` — 값 범위 제약

숫자 속성의 *허용 범위*를 강제한다.

```typeql
define
  relation supplies,
    relates supplier,
    relates customer,
    relates supplied_product,
    owns revenue_share @range(0.0..1.0);
  
  attribute revenue_share, value double;
```

`@range(0.0..1.0)`은 *0과 1 사이의 값*만 허용한다. *매출 비중*은 의미상 0~100%(즉, 0.0~1.0) 사이여야 한다. 누군가 실수로 *1.5*를 넣으려고 하면, 데이터베이스가 거부한다.

`@range`는 도메인 의미의 일부분이다. *매출 비중은 100%를 넘을 수 없다*는 사실이 — 코드의 *if*에 들어가지 않고, *데이터 모델*에 박힌다.

### `@values` — 허용된 값 집합

`@values`는 *허용된 값 목록*을 정의한다.

```typeql
define
  attribute market_segment_type, value string @values("HBM", "DRAM", "NAND", "Logic", "Optical");
  
  market_segment owns market_segment_type;
```

`market_segment_type` 속성은 *다섯 가지 값 중 하나*만 가질 수 있다. *Memory*나 *AI* 같은 다른 값은 거부된다. 도메인의 *카테고리 체계*가 데이터 모델에 박힌다.

`@values`는 *분류*의 더 가벼운 버전으로 작동한다. *Entity 상속*으로 표현하기엔 너무 가벼운 카테고리 — 가령 *시장 세그먼트 같은 간단한 라벨* — 는 `@values`로 처리하는 게 자연스럽다.

---

## 7.4 도메인 규칙 강제 — 잘못된 데이터 거부 시연

위의 세 가지 제약이 *실제로 어떻게 일을 하는가*. 한 가지 시연을 본다.

스키마에 제약을 추가한다.

```typeql
define
  # 회사는 이름을 정확히 하나 가져야 한다
  entity company,
    owns name @card(1..1),
    owns ticker @card(0..1);
  
  # 공급 관계는 단일 공급원 여부 속성을 가질 수 있다
  relation supplies,
    relates supplier @card(1..1),
    relates customer @card(1..1),
    relates supplied_product @card(1..1),
    owns since_year @card(1..1),
    owns is_sole_source @card(0..1);
  
  attribute is_sole_source, value boolean;

commit;
```

여기서 `relates supplier @card(1..1)`은 *한 공급 사건에는 공급자가 정확히 한 명*이라는 약속이다. *두 명의 공급자가 함께 한 사건에 묶이는* 데이터는 거부된다. (반면 *경쟁 관계*는 `relates competitor @card(1..)` — 여러 경쟁자가 함께 들어갈 수 있어야 한다.)

이제 *잘못된 데이터*를 넣어 본다.

```typeql
insert
  $bad_company isa company,
    has name "회사 A",
    has name "회사 B";
```

같은 회사가 이름을 *두 개* 가지는 데이터다. 트랜잭션을 커밋하면 — 데이터베이스가 거부한다.

```
Error: Cardinality violation. Entity 'company' has attribute 'name' with cardinality 2, exceeds maximum 1.
```

오류 메시지가 *정확히 어디서 무엇이 잘못됐는지* 알려준다. *cardinality 2, exceeds maximum 1.* — 이름이 두 개인데 최대 한 개만 허용된다.

같은 모양으로, *매출 비중이 1.5인 데이터*도 거부된다.

```typeql
insert
  $bad_supply (supplier: $hynix, customer: $nvda, supplied_product: $hbm3e)
    isa supplies,
    has revenue_share 1.5;
```

오류:

```
Error: Range violation. Attribute 'revenue_share' value 1.5 outside range [0.0, 1.0].
```

도메인 규칙이 — *코드의 if 문이 아니라* 데이터베이스에서 — 강제된다. 새 분석가가 이 데이터베이스를 처음 열어서 데이터를 넣을 때, *도메인 규칙을 모르고 잘못된 값을 넣어도* 데이터베이스가 자기 자신을 지킨다.

이게 *명시적 약속이 데이터 모델 안에 박혀 있다*는 사실의 진짜 가치다. 약속이 *코드 안에 흩어져 있으면* — 새 분석가가 약속을 모를 수 있다. 약속이 *데이터 모델에 박혀 있으면* — 데이터베이스가 약속의 *수호자*가 된다.

---

## 7.5 좋은 스키마 vs 나쁜 스키마

이 장에서 본 사고법들을 종합해서, 같은 도메인의 *좋은 스키마*와 *나쁜 스키마*를 비교해 본다.

### 나쁜 스키마

```typeql
define
  entity thing,
    owns name,
    owns category,
    owns related_to;
  
  attribute name, value string;
  attribute category, value string;
  attribute related_to, value string;
```

모든 개체가 *thing*이고, 분류는 *category*라는 문자열 속성에 들어가고, 관계는 *related_to*라는 문자열에 *대상의 이름을 적는* 방식이다.

이게 왜 나쁜가.

첫째, *분류*가 데이터 모델 안에 없다. *category*가 어떤 값을 가질 수 있는지 약속이 없다. 누군가는 *광반도체*를, 다른 누군가는 *optical semiconductor*를, 또 다른 누군가는 *광반도체 회사*를 적는다. 같은 의미가 서로 다른 문자열로 흩어진다.

둘째, *관계*가 *문자열*이다. *related_to* 컬럼에 *"NVIDIA"*라고 적는 게 — *그 NVIDIA가 데이터베이스 안의 어느 회사인지*는 알 수 없다. 두 NVIDIA가 있다면 어느 NVIDIA를 가리키는지 모호하다.

셋째, *자리*가 없다. 누가 공급자이고 누가 수요자인지 — *related_to* 안에 들어가는 회사명만 보고는 알 수 없다.

이 스키마 위에서는 — *분석*이 불가능하다. 데이터가 들어 있지만, 의미가 표현되지 않는다.

### 좋은 스키마

같은 도메인을, 이 장과 6장의 사고법으로 짠다.

```typeql
define
  # 분류 — 다층
  entity company,
    owns name @card(1..1),
    owns ticker @card(0..1);
  
  entity ai_chip_designer sub company;
  entity hbm_maker sub company;
  entity optical_component_maker sub company;
  entity optical_semiconductor_maker sub optical_component_maker;
  entity optical_fiber_maker sub optical_component_maker;
  entity optical_installation_equipment_maker sub optical_component_maker;
  
  # 제품 — 분류
  entity product,
    owns model_name @card(1..1);
  
  entity memory_chip sub product;
  entity optical_component sub product;
  
  # 공급 관계 — 자리와 제약
  relation supplies,
    relates supplier @card(1..1),
    relates customer @card(1..1),
    relates supplied_product @card(1..1),
    owns since_year @card(1..1),
    owns revenue_share @card(0..1);
  
  # 자리 정의
  company plays supplies:supplier;
  company plays supplies:customer;
  product plays supplies:supplied_product;
  
  # 속성과 값 범위
  attribute name, value string;
  attribute ticker, value string;
  attribute model_name, value string;
  attribute since_year, value long @range(2000..2100);
  attribute revenue_share, value double @range(0.0..1.0);

commit;
```

같은 도메인이지만 — *모든 것이 명시적*이다.

- *어떤 회사 종류가 있을 수 있는가*가 분류 가지로 표현됨
- *어떤 제품 종류가 있을 수 있는가*가 분류 가지로 표현됨
- *공급이 무엇인가*가 N항 관계로 표현됨
- *공급의 자리들*이 명시적으로 적힘
- *회사가 공급자 자리에 들어갈 수 있다*는 약속이 plays로 강제됨
- *매출 비중은 0~1 사이여야 한다*는 약속이 @range로 강제됨

이 스키마 위에서는 — *분석이 자연스럽다*. 그리고 6개월 뒤에 새 분석가가 데이터베이스를 처음 열어 봐도 — *이 도메인이 어떻게 짜여 있는지*가 스키마만 보고도 보인다.

**좋은 스키마의 핵심은 *명시성*이다.** 도메인의 모든 약속이 — 코드 안이나 사람의 머릿속이 아니라 — 스키마 자체에 적혀 있다.

---

## 7.6 마무리 — 8장으로의 다리

이 장에서 한 일을 정리하면 다음과 같다. *분류 가지를 어디까지 쪼갤 것인가*에 대한 사고법, *같은 회사가 여러 자리에 들어가는 모양*을 plays로 표현하는 방법, `@card`·`@range`·`@values`라는 세 가지 제약, 그리고 *좋은 스키마와 나쁜 스키마*의 차이.

이제 스키마라는 *그릇*이 충분히 단단해졌다. 그러나 — 그릇이 단단해도 *그 안의 작업*이 없으면 분석은 일어나지 않는다.

다음 장은 *작업*의 자리다. **Function** — TypeDB 3.0의 추론 도구. 단순한 조회를 넘어, *데이터 위에서 다단계 추론*을 한다. *NVIDIA의 직접 공급사*뿐 아니라 *NVIDIA의 모든 직간접 공급사*를 추적한다. *대장주가 움직인 뒤 후속 노드가 아직 안 움직인 자리*를 가리킨다. 1편 회상의 *시차 알파 시스템*이 — Function 위에서 자라난다.

그릇과 작업이 함께 움직이기 시작하면, 데이터베이스는 *지식 그래프*로 변환된다. 다음 장이 그 변환의 첫 자리다.
