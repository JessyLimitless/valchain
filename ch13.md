# 제13장. 기술 채택 발언 관계 — 신호의 출발점이 데이터로

## 들어가며

12장 끝에서 나는 한 가지를 적어두고 닫았다. *광전자, 이노인스트루먼트, 대한광통신은 NVIDIA의 직간접 공급사 명단에 없다. 신호는 공급의 흐름이 아니라 기술 채택의 흐름을 통해 그들에게 도달한다.*

이 장이 그 *기술 채택의 흐름*을 데이터로 짜는 자리다.

NVIDIA가 *어떤 기술을 핵심 기술로 채택했다*고 발언하는 사건은 — 공급 관계와는 다른 종류의 신호다. 공급 관계는 *이미 일어난 거래*다. 기술 채택 발언은 *앞으로 일어날 거래의 방향*을 알리는 사건이다. 발언이 *공급망 안의 회사*에만 영향을 주는 게 아니라 — *그 발언이 가리키는 기술과 관련된 모든 회사*에 신호를 보낸다. 광전자, 이노인스트루먼트, 대한광통신이 NVIDIA의 직간접 공급사가 아닌데도 신호가 도달한 *유일한 메커니즘*이 이 흐름이다.

이 장의 끝에서, 1편 회상의 *NVIDIA GTC 2026년 4월 광반도체 발언*이 데이터로 박힌다. 그리고 그 발언이 *영향을 미치는 회사 후보 전체*가 — 자동으로 도출된다. 광전자, 이노인스트루먼트, 대한광통신이 처음으로 *데이터의 신호 경로 위*에 자리를 찾는다.

---

## 13.1 `technology_endorsement` — N항 관계의 가장 풍부한 모양

기술 채택 발언은 — 2장 회상에서 처음 모양이 잡힌 사건이다. *발언자, 시점, 지목 기술, 전파 경로, 영향 받는 회사 후보*. 일곱 가지가 한 매듭에 묶이는 그 사건. 이게 — TypeDB N항 관계의 가장 풍부한 표현력이 발휘되는 자리다.

스키마 모드에서 정의한다.

```typeql
transaction schema optical_chain
```

```typeql
define
  relation technology_endorsement,
    relates announcer @card(1..1),
    relates endorsed_technology @card(1..1),
    relates beneficiary_company @card(0..),
    owns announcement_date @card(1..1),
    owns announcement_venue @card(0..1),
    owns endorsement_strength @card(0..1);
  
  company plays technology_endorsement:announcer;
  technology plays technology_endorsement:endorsed_technology;
  company plays technology_endorsement:beneficiary_company;
  
  attribute announcement_date, value date;
  attribute announcement_venue, value string;
  attribute endorsement_strength, value string @values("strong", "moderate", "weak");

commit;
```

이 관계가 — 2장에서 회상한 일곱 가지를 모두 담는다.

**자리 1 — `announcer`**: 발언한 주체. 회사. *@card(1..1)*로 *반드시 한 회사가 발언*한다는 강제. 한 매듭에 두 회사가 *동시 발언*하는 모양은 두 개의 별도 매듭으로 짠다.

**자리 2 — `endorsed_technology`**: 지목된 기술. 11장에서 짜둔 기술 트리의 어느 한 가지. *@card(1..1)* — 한 사건에 *하나의 핵심 기술*을 지목하는 모양. 여러 기술을 지목한 발언은 — 각각의 매듭으로 쪼개진다.

**자리 3 — `beneficiary_company`**: 영향 받는 회사들. 여러 명. *@card(0..)*로 *영(0)개 이상*. 발언 직후에는 *영향 받는 회사가 누구인지 모를 수도 있다*는 점이 — 카디널리티에 반영됐다. 그러나 보통의 경우 *분석 결과로 도출된 후보들*이 이 자리에 묶인다.

**속성들**: `announcement_date`는 *언제* 발언이 일어났는지 (시점이 14장의 핵심이 된다). `announcement_venue`는 *어디서* — GTC, CES, 컨퍼런스 콜, IR 자료 발표 등. `endorsement_strength`는 *발언의 강도* — `@values`로 *strong, moderate, weak* 세 단계를 강제. 단순한 *기술 언급*과 *공식 채택 발표*를 가르는 자리다.

이 한 관계가 — 책의 후반 모든 *신호 분석*의 토대다.

---

## 13.2 NVIDIA 2026년 4월 광반도체 발언

1편 회상의 핵심 사건. 2026년 4월, NVIDIA 젠슨 황 CEO가 GTC 키노트에서 광반도체를 미래 핵심 기술로 지목한 발언. 이게 데이터로 들어간다.

쓰기 모드로 전환.

```typeql
transaction write optical_chain
```

```typeql
match
  $nvda isa company, has name "NVIDIA";
  $cpo_tech_inst isa cpo_tech, has technology_name "Co-Packaged Optics";

insert
  $event (
    announcer: $nvda,
    endorsed_technology: $cpo_tech_inst
  ) isa technology_endorsement,
    has announcement_date 2026-04-01,
    has announcement_venue "GTC 2026 Keynote",
    has endorsement_strength "strong";

commit;
```

한 매듭이 박혔다. *NVIDIA가 2026년 4월 1일 GTC 2026 키노트에서 Co-Packaged Optics를 강하게 지목했다*는 사실이 — 한 줄의 데이터 단위로 적혔다. 1편 회상의 그 사건이 — 머릿속도 아니고 종이도 아니고 — 데이터베이스의 자기 자리에.

여기서 짚어둘 정직한 사실 하나. **위 발언의 정확한 시점·장소·강도는 분석가의 해석에 따라 차이가 있다.** GTC 2026 키노트의 정확한 진행 일자, 광반도체 지목의 *강도가 strong인지 moderate인지*에 대한 평가는 — 산업 분석가마다 다르게 본다. 이 책의 분석은 *책의 시나리오*에 맞춰 짜인 *대표적 해석*이고, 실제 분석에 쓰려면 *공식 발표 자료, GTC 키노트 영상, 분석가 리포트* 같은 원자료를 별도의 *증거 매듭*으로 박는 작업이 필요하다. 14장에서 그 *증거 자리*도 함께 짚는다.

지금은 — *발언 자체가 데이터에 박혔다*는 사실이 중요하다. 다음 절부터, 이 한 매듭에서 *신호 경로*가 자동으로 펼쳐진다.

---

## 13.3 발언이 영향을 미치는 회사 후보 — 자동 도출 함수

위의 매듭에서 `beneficiary_company` 자리는 *아직 비어 있다*. 발언 자체는 데이터에 있지만, *그 발언이 영향을 미치는 회사가 누구인지*는 — 자동으로 도출되어야 한다. 그게 책의 약속이었다.

도출의 논리는 다음과 같다. *지목된 기술과 관련된 제품을 만드는 모든 회사*가 영향 받는 회사 후보다. CPO 기술이 지목되면 — CPO 기술을 *핵심으로 사용하는 제품*을 만드는 회사뿐 아니라, *CPO 기술과 관련된 광 부품*을 만드는 회사들도 영향 받는다. 광반도체, 광섬유, 광통신 시공 장비 — 1편 회상의 세 회사가 만드는 모든 것이 *CPO 인프라의 한 부분*이기 때문이다.

이걸 함수로 짠다. 단계가 두 가지다.

**첫째 — 지목 기술과 같은 분류의 모든 기술.** CPO만이 아니라 *광 인터커넥트* 가지 전체. 그러면 LPO, 그리고 *광 인터커넥트 상위 가지*에 속한 모든 기술이 포함된다.

**둘째 — 그 기술들이 영향을 미치는 제품 분류, 그 제품들을 만드는 회사 전체.** 광 인터커넥트 → 광 부품 → 광 부품을 만드는 모든 회사.

스키마 모드에서 두 함수를 정의한다.

```typeql
transaction schema optical_chain
```

```typeql
define
  # 함수 1 — 지목 기술과 같은 가지의 기술들 (분류 상속을 활용)
  fun related_technologies($t: technology) -> { technology }:
    match
      $sibling isa technology;
      { $sibling is $t; } or
      { $sibling isa $type;
        $t isa $type;
        $type sub! technology; };
    return { $sibling };
  
  # 함수 2 — 한 기술의 모든 영향 회사 (광 인터커넥트 → 광 부품 메이커)
  fun beneficiaries_of_technology($t: technology) -> { company }:
    match
      let $related in related_technologies($t);
      $company isa optical_component_maker;
    return { $company };

commit;
```

여기서 한 가지 설명이 필요하다. 두 번째 함수는 *지금은 단순화된 버전*이다. CPO 같은 *광 인터커넥트* 기술이 지목되면 *광 부품 메이커 전체*가 영향 받는다는 가정. 실제 분석에서는 더 정교한 함수가 필요하다 — *어떤 광 부품이 광 인터커넥트와 직접 관련되는지*를 *제품-기술 매듭*을 통해 추적하는 모양. 이런 정교한 버전은 5편 *알파 발굴*에서 본격적으로 짜인다.

이 장에서는 *단순화된 버전*으로 충분하다. 핵심은 — *어떻게 분류 가지를 따라 신호 경로가 자동으로 펼쳐지는가*를 보이는 것이다.

호출해 본다.

```typeql
transaction read optical_chain
```

```typeql
match
  $cpo isa cpo_tech, has technology_name "Co-Packaged Optics";
  let $beneficiary in beneficiaries_of_technology($cpo);
  $beneficiary has name $name;
fetch
  $name;
```

답:

```json
{ "name": "광전자" }
{ "name": "대한광통신" }
{ "name": "이노인스트루먼트" }
{ "name": "Lumentum" }
{ "name": "Coherent" }
```

**다섯 회사가 답으로 나왔다.** 한국 3개(광전자, 대한광통신, 이노인스트루먼트) + 미국 2개(Lumentum, Coherent). 모두 — 10장에서 *L6 광 부품 메이커* 가지에 넣어둔 회사들이다.

이 답이 *결정적*인 이유는 — 12장에서는 *광전자, 이노인스트루먼트, 대한광통신이 NVIDIA의 직간접 공급사 명단에 없었다*는 사실이다. 그런데 이 장에서는 — *NVIDIA의 광반도체 발언이라는 사건*을 통해, 그 세 회사가 *처음으로 데이터의 신호 경로 위*에 자리잡았다.

이게 — 1편 회상의 결정적 한 자리가 데이터로 실현된 모양이다.

---

## 13.4 결정적 자리 — 세 회사가 신호 경로 위에 자리잡다

위의 도출 결과를 — *기존 발언 매듭의 `beneficiary_company` 자리에 박는다*. 도출이 데이터에 영구히 저장되도록.

```typeql
transaction write optical_chain
```

```typeql
match
  $event isa technology_endorsement, has announcement_date 2026-04-01;
  $cpo isa cpo_tech, has technology_name "Co-Packaged Optics";
  
  let $beneficiary in beneficiaries_of_technology($cpo);

insert
  $event links (beneficiary_company: $beneficiary);

commit;
```

여기서 `links`는 *기존 관계 인스턴스에 새 자리 참여자를 추가*하는 모양이다. 한 매듭이 *영(0)개의 beneficiary로 시작*해서, *다섯 개의 beneficiary가 박힌* 매듭으로 자라났다.

확인한다.

```typeql
transaction read optical_chain
```

```typeql
match
  $event isa technology_endorsement,
    has announcement_date $date,
    has announcement_venue $venue,
    has endorsement_strength $strength;
  
  (announcer: $announcer, endorsed_technology: $tech, beneficiary_company: $beneficiary) isa technology_endorsement;
  $event is $event;
  
  $announcer has name $announcer_name;
  $tech has technology_name $tech_name;
  $beneficiary has name $beneficiary_name;

fetch
  $announcer_name; $tech_name; $beneficiary_name; $date; $strength;
```

답 (5행 모두):

```json
{ "announcer_name": "NVIDIA", "tech_name": "Co-Packaged Optics", 
  "beneficiary_name": "광전자", "date": "2026-04-01", "strength": "strong" }
{ "announcer_name": "NVIDIA", "tech_name": "Co-Packaged Optics", 
  "beneficiary_name": "대한광통신", "date": "2026-04-01", "strength": "strong" }
{ "announcer_name": "NVIDIA", "tech_name": "Co-Packaged Optics", 
  "beneficiary_name": "이노인스트루먼트", "date": "2026-04-01", "strength": "strong" }
{ "announcer_name": "NVIDIA", "tech_name": "Co-Packaged Optics", 
  "beneficiary_name": "Lumentum", "date": "2026-04-01", "strength": "strong" }
{ "announcer_name": "NVIDIA", "tech_name": "Co-Packaged Optics", 
  "beneficiary_name": "Coherent", "date": "2026-04-01", "strength": "strong" }
```

한 사건이 — *발언자, 시점, 장소, 강도, 지목 기술, 영향 받는 회사들* 일곱 가지를 모두 담은 채로 — 데이터 안에 자리잡았다. 그리고 그 사건의 *영향 회사 자리*에 — 1편 회상의 세 한국 회사 모두가 *함께* 묶였다.

여기서 잠깐 멈춰서 — 1편 1장의 한 단락을 다시 떠올린다.

> *광전자와 이노인스트루먼트는 같은 자리에 있었다. 같은 NVIDIA 광 인터커넥트 경로 위의 두 회사. 두 회사가 그 자리에 있어야 할 자격은 동등했다. 그런데 한 회사는 내 머릿속에 자리잡고 있었고, 다른 회사는 없었다.*

그 *같은 자리*가 — 이제 데이터 안의 *같은 매듭의 같은 role*이 됐다. 머릿속 그림이 어쩌다 한 회사만 외워둔 비대칭은 — 데이터의 그림에서는 *원리적으로 불가능*하다. *광 부품 메이커 가지에 속한 회사*는 모두 — *cpo_tech 발언의 beneficiary 자리*에 자동으로 묶인다. 외워둘 필요가 없다. 시스템이 묶어준다.

대한광통신도 같은 자리에 있다. 후배가 *대장주가 먼저 가고 나머지가 따라온다*고 했을 때, 그 말이 가리키던 그 세 회사 — 광전자, 이노인스트루먼트, 대한광통신 — 이 모두 *같은 사건의 같은 자리*에 묶인 모양으로 데이터에 박혔다.

이게 — 이 책이 시작될 때 약속했던 *바로 그 자리*다.

---

## 13.5 다른 발언도 같은 모양으로

이 장의 패턴은 — *NVIDIA 광반도체 발언*에만 적용되지 않는다. 모든 *기술 채택 발언*이 같은 모양으로 데이터에 박힌다.

NVIDIA가 *HBM4를 차세대 칩의 핵심으로 채택할 것*이라고 발언한 사건을 추가로 박아 본다.

```typeql
transaction write optical_chain
```

```typeql
match
  $nvda isa company, has name "NVIDIA";
  $hbm_tech isa hbm_technology, has technology_name "HBM3E";
  # (실제로는 HBM4 기술 entity가 따로 정의되어야 함; 단순화를 위해 HBM3E 사용)

insert
  $event2 (
    announcer: $nvda,
    endorsed_technology: $hbm_tech
  ) isa technology_endorsement,
    has announcement_date 2024-03-18,
    has announcement_venue "GTC 2024 Keynote",
    has endorsement_strength "strong";

commit;
```

같은 매듭의 모양이다. 그러나 — *영향 받는 회사*는 *다른 분류 가지*가 된다. HBM 관련 발언이라면 *광 부품 메이커*가 아니라 *HBM 메이커*가 beneficiary로 묶인다.

함수도 다른 자리를 도출해야 한다. *광 부품* 가지가 아니라 *HBM 가지*로. 실제로는 — *한 함수가 지목 기술의 종류에 따라 다른 분류 가지를 자동 선택*하는 정교한 버전이 필요하다. 이게 5편 *알파 발굴*에서 본격적으로 짜이는 자리다.

지금은 핵심만 짚는다 — *같은 사건 모양*이 *다른 도메인 가지*로 자동 펼쳐진다는 사실. 한 번 짠 *technology_endorsement* 관계와 *beneficiaries_of_technology* 함수가, *어떤 사건에도 같은 모양으로 작동*한다.

---

## 13.6 마무리 — 14장으로의 다리

이 장에서 한 일을 정리한다. *technology_endorsement* 관계의 본격 정의 (일곱 가지를 한 매듭에 묶는 N항 관계), NVIDIA 2026년 4월 광반도체 발언이 데이터로, *beneficiaries_of_technology* 함수로 영향 회사 자동 도출, 그리고 — 1편 회상의 세 회사가 *처음으로* 데이터의 신호 경로 위에 자리잡음.

그러나 — 이 데이터에서 *아직 빠진 한 가지*가 있다. **시점.** 발언이 *언제 일어났는지*는 데이터에 있다. 그러나 *영향을 받는 다섯 회사가 언제 어떻게 가격으로 반응했는지*는 — 아직 데이터에 없다. 대한광통신이 1년 먼저 움직였고, 광전자가 4월 1일에 갭 상승했고, 이노인스트루먼트가 4월 7일에 첫 상한가를 친 그 *시간의 차이*가 — 데이터로 박혀 있어야 한다.

다음 장이 그 자리다. *significant_price_move* 관계가 들어가고, 세 회사의 핵심 가격 움직임이 데이터로 박힌다. 그 위에서 — *대장주가 누구인지*, *후속 노드가 어디 있는지*가 데이터 안에서 보이기 시작한다. 1편 회상의 *시차 알파*의 데이터 토대가 14장에서 완성된다. 15장의 본격 분석이 그 위에서 시작된다.

회상이 사실이 되는 자리. 신호의 출발점이 박혔으니, 다음 장에서 *그 신호가 시간 위에서 어떻게 펼쳐졌는지*가 박힌다.
